// zlink/ptr_map.cpp — Pointer mapping implementation
// (Most of the implementation is in the header since it's template-heavy.
//  This file contains only the non-inline utilities.)

#include <zlink/ptr_map.hpp>

// The ptr_map class is fully implemented in the header as it's
// template-independent and header-only for performance.
// This translation unit exists for any future non-inline additions.

namespace zlink {
// Placeholder — all methods are inline in the header
} // namespace zlink
