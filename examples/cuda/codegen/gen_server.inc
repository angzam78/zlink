// AUTO-GENERATED by codegen/codegen.py — DO NOT EDIT
// Contains 269 CUDA Driver API server handler bodies
// Included in cuda_server_codegen.cpp

// ── cuInit (server handler) ──
// cuInit → real CUDA call on GPU
static InitRet handle_init(uint32_t Flags) {
    CUresult res = cuInit(static_cast<unsigned int>(Flags));
    InitRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDeviceGetCount (server handler) ──
// cuDeviceGetCount → real CUDA call on GPU
static DeviceGetCountRet handle_device_get_count() {
    int real_count = 0;
    CUresult res = cuDeviceGetCount(&real_count);
    DeviceGetCountRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.count = static_cast<int32_t>(real_count);
    return ret;
}

// ── cuDeviceGet (server handler) ──
// cuDeviceGet → real CUDA call on GPU
static DeviceGetRet handle_device_get(int32_t ordinal) {
    CUresult res = cuDeviceGet(&real_device, ordinal);
    DeviceGetRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.device = static_cast<int32_t>(real_device);
    return ret;
}

// ── cuDeviceGetName (server handler) ──
// cuDeviceGetName → real CUDA call on GPU
static DeviceGetNameRet handle_device_get_name(int32_t len, int32_t dev) {
    char real_name[256] = {};
    CUresult res = cuDeviceGetName(&real_name, len, static_cast<CUdevice>(dev));
    DeviceGetNameRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDeviceTotalMem (server handler) ──
// cuDeviceTotalMem → real CUDA call on GPU
static DeviceTotalMemRet handle_device_total_mem(int32_t dev) {
    size_t real_bytes = 0;
    CUresult res = cuDeviceTotalMem(&real_bytes, static_cast<CUdevice>(dev));
    DeviceTotalMemRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.bytes = static_cast<uint64_t>(real_bytes);
    return ret;
}

// ── cuDeviceTotalMem_v2 (server handler) ──
// cuDeviceTotalMem_v2 → real CUDA call on GPU
static DeviceTotalMemV2Ret handle_device_total_mem_v2(int32_t dev) {
    size_t real_bytes = 0;
    CUresult res = cuDeviceTotalMem_v2(&real_bytes, static_cast<CUdevice>(dev));
    DeviceTotalMemV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.bytes = static_cast<uint64_t>(real_bytes);
    return ret;
}

// ── cuDeviceGetAttribute (server handler) ──
// cuDeviceGetAttribute → real CUDA call on GPU
static DeviceGetAttributeRet handle_device_get_attribute(uint32_t attrib, int32_t dev) {
    int real_value = 0;
    CUresult res = cuDeviceGetAttribute(&real_value, static_cast<unsigned int>(attrib), static_cast<CUdevice>(dev));
    DeviceGetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<int32_t>(real_value);
    return ret;
}

// ── cuDeviceGetMemPool (server handler) ──
// cuDeviceGetMemPool → real CUDA call on GPU
static DeviceGetMemPoolRet handle_device_get_mem_pool(int32_t dev) {
    CUmemGenericAllocationHandle real_pool = nullptr;
    CUresult res = cuDeviceGetMemPool(&real_pool, static_cast<CUdevice>(dev));
    DeviceGetMemPoolRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pool = reinterpret_cast<uint64_t>(real_pool);
    if (res == CUDA_SUCCESS && real_pool != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuDeviceSetMemPool (server handler) ──
// cuDeviceSetMemPool → real CUDA call on GPU
static DeviceSetMemPoolRet handle_device_set_mem_pool(int32_t dev, uint64_t pool) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUresult res = cuDeviceSetMemPool(static_cast<CUdevice>(dev), real_pool);
    DeviceSetMemPoolRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDeviceGetExecAffinitySupport (server handler) ──
// cuDeviceGetExecAffinitySupport → real CUDA call on GPU
static DeviceGetExecAffinitySupportRet handle_device_get_exec_affinity_support(uint32_t flag, int32_t dev) {
    int real_pi = 0;
    CUresult res = cuDeviceGetExecAffinitySupport(&real_pi, static_cast<unsigned int>(flag), static_cast<CUdevice>(dev));
    DeviceGetExecAffinitySupportRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pi = static_cast<int32_t>(real_pi);
    return ret;
}

// ── cuDeviceGetCudaComputeCapability (server handler) ──
// cuDeviceGetCudaComputeCapability → real CUDA call on GPU
static DeviceGetCudaComputeCapabilityRet handle_device_get_cuda_compute_capability(int32_t dev) {
    int real_major = 0;
    int real_minor = 0;
    CUresult res = cuDeviceGetCudaComputeCapability(&real_major, &real_minor, static_cast<CUdevice>(dev));
    DeviceGetCudaComputeCapabilityRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.major = static_cast<int32_t>(real_major);
    ret.minor = static_cast<int32_t>(real_minor);
    return ret;
}

// ── cuDevicePrimaryCtxRetain (server handler) ──
// cuDevicePrimaryCtxRetain → real CUDA call on GPU
static DevicePrimaryCtxRetainRet handle_device_primary_ctx_retain(int32_t dev) {
    CUcontext real_pctx = nullptr;
    CUresult res = cuDevicePrimaryCtxRetain(&real_pctx, static_cast<CUdevice>(dev));
    DevicePrimaryCtxRetainRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuDevicePrimaryCtxRelease (server handler) ──
// cuDevicePrimaryCtxRelease → real CUDA call on GPU
static DevicePrimaryCtxReleaseRet handle_device_primary_ctx_release(int32_t dev) {
    CUresult res = cuDevicePrimaryCtxRelease(static_cast<CUdevice>(dev));
    DevicePrimaryCtxReleaseRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDevicePrimaryCtxRelease_v2 (server handler) ──
// cuDevicePrimaryCtxRelease_v2 → real CUDA call on GPU
static DevicePrimaryCtxReleaseV2Ret handle_device_primary_ctx_release_v2(int32_t dev) {
    CUresult res = cuDevicePrimaryCtxRelease_v2(static_cast<CUdevice>(dev));
    DevicePrimaryCtxReleaseV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDevicePrimaryCtxSetFlags (server handler) ──
// cuDevicePrimaryCtxSetFlags → real CUDA call on GPU
static DevicePrimaryCtxSetFlagsRet handle_device_primary_ctx_set_flags(int32_t dev, uint32_t flags) {
    CUresult res = cuDevicePrimaryCtxSetFlags(static_cast<CUdevice>(dev), static_cast<unsigned int>(flags));
    DevicePrimaryCtxSetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDevicePrimaryCtxSetFlags_v2 (server handler) ──
// cuDevicePrimaryCtxSetFlags_v2 → real CUDA call on GPU
static DevicePrimaryCtxSetFlagsV2Ret handle_device_primary_ctx_set_flags_v2(int32_t dev, uint32_t flags) {
    CUresult res = cuDevicePrimaryCtxSetFlags_v2(static_cast<CUdevice>(dev), static_cast<unsigned int>(flags));
    DevicePrimaryCtxSetFlagsV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDevicePrimaryCtxGetState (server handler) ──
// cuDevicePrimaryCtxGetState → real CUDA call on GPU
static DevicePrimaryCtxGetStateRet handle_device_primary_ctx_get_state(int32_t dev) {
    unsigned int real_flags = 0;
    int real_active = 0;
    CUresult res = cuDevicePrimaryCtxGetState(&real_flags, &real_active, static_cast<CUdevice>(dev));
    DevicePrimaryCtxGetStateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.flags = static_cast<uint32_t>(real_flags);
    ret.active = static_cast<int32_t>(real_active);
    return ret;
}

// ── cuCtxCreate (server handler) ──
// cuCtxCreate → real CUDA call on GPU
static CtxCreateRet handle_ctx_create(uint32_t flags, int32_t dev) {
    CUcontext real_pctx = nullptr;
    CUresult res = cuCtxCreate(&real_pctx, static_cast<unsigned int>(flags), static_cast<CUdevice>(dev));
    CtxCreateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuCtxCreate_v2 (server handler) ──
// cuCtxCreate_v2 → real CUDA call on GPU
static CtxCreateV2Ret handle_ctx_create_v2(uint32_t flags, int32_t dev) {
    CUcontext real_pctx = nullptr;
    CUresult res = cuCtxCreate_v2(&real_pctx, static_cast<unsigned int>(flags), static_cast<CUdevice>(dev));
    CtxCreateV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuCtxDestroy (server handler) ──
// cuCtxDestroy → real CUDA call on GPU
static CtxDestroyRet handle_ctx_destroy(uint64_t ctx) {
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    CUresult res = cuCtxDestroy(real_ctx);
    CtxDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxDestroy_v2 (server handler) ──
// cuCtxDestroy_v2 → real CUDA call on GPU
static CtxDestroyV2Ret handle_ctx_destroy_v2(uint64_t ctx) {
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    CUresult res = cuCtxDestroy_v2(real_ctx);
    CtxDestroyV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxSetCurrent (server handler) ──
// cuCtxSetCurrent → real CUDA call on GPU
static CtxSetCurrentRet handle_ctx_set_current(uint64_t ctx) {
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    CUresult res = cuCtxSetCurrent(real_ctx);
    CtxSetCurrentRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxGetCurrent (server handler) ──
// cuCtxGetCurrent → real CUDA call on GPU
static CtxGetCurrentRet handle_ctx_get_current() {
    CUcontext real_pctx = nullptr;
    CUresult res = cuCtxGetCurrent(&real_pctx);
    CtxGetCurrentRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuCtxPushCurrent (server handler) ──
// cuCtxPushCurrent → real CUDA call on GPU
static CtxPushCurrentRet handle_ctx_push_current(uint64_t ctx) {
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    CUresult res = cuCtxPushCurrent(real_ctx);
    CtxPushCurrentRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxPushCurrent_v2 (server handler) ──
// cuCtxPushCurrent_v2 → real CUDA call on GPU
static CtxPushCurrentV2Ret handle_ctx_push_current_v2(uint64_t ctx) {
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    CUresult res = cuCtxPushCurrent_v2(real_ctx);
    CtxPushCurrentV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxPopCurrent (server handler) ──
// cuCtxPopCurrent → real CUDA call on GPU
static CtxPopCurrentRet handle_ctx_pop_current() {
    CUcontext real_pctx = nullptr;
    CUresult res = cuCtxPopCurrent(&real_pctx);
    CtxPopCurrentRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuCtxPopCurrent_v2 (server handler) ──
// cuCtxPopCurrent_v2 → real CUDA call on GPU
static CtxPopCurrentV2Ret handle_ctx_pop_current_v2() {
    CUcontext real_pctx = nullptr;
    CUresult res = cuCtxPopCurrent_v2(&real_pctx);
    CtxPopCurrentV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuCtxSetFlags (server handler) ──
// cuCtxSetFlags → real CUDA call on GPU
static CtxSetFlagsRet handle_ctx_set_flags(uint32_t flags) {
    CUresult res = cuCtxSetFlags(static_cast<unsigned int>(flags));
    CtxSetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxSetFlags_v2 (server handler) ──
// cuCtxSetFlags_v2 → real CUDA call on GPU
static CtxSetFlagsV2Ret handle_ctx_set_flags_v2(uint32_t flags) {
    CUresult res = cuCtxSetFlags_v2(static_cast<unsigned int>(flags));
    CtxSetFlagsV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxSynchronize (server handler) ──
// cuCtxSynchronize → real CUDA call on GPU
static CtxSynchronizeRet handle_ctx_synchronize() {
    CUresult res = cuCtxSynchronize();
    CtxSynchronizeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxGetDevice (server handler) ──
// cuCtxGetDevice → real CUDA call on GPU
static CtxGetDeviceRet handle_ctx_get_device() {
    CUresult res = cuCtxGetDevice(&real_device);
    CtxGetDeviceRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.device = static_cast<int32_t>(real_device);
    return ret;
}

// ── cuCtxGetFlags (server handler) ──
// cuCtxGetFlags → real CUDA call on GPU
static CtxGetFlagsRet handle_ctx_get_flags() {
    unsigned int real_flags = 0;
    CUresult res = cuCtxGetFlags(&real_flags);
    CtxGetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.flags = static_cast<uint32_t>(real_flags);
    return ret;
}

// ── cuCtxGetStreamPriorityRange (server handler) ──
// cuCtxGetStreamPriorityRange → real CUDA call on GPU
static CtxGetStreamPriorityRangeRet handle_ctx_get_stream_priority_range() {
    int real_leastPriority = 0;
    int real_greatestPriority = 0;
    CUresult res = cuCtxGetStreamPriorityRange(&real_leastPriority, &real_greatestPriority);
    CtxGetStreamPriorityRangeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.leastPriority = static_cast<int32_t>(real_leastPriority);
    ret.greatestPriority = static_cast<int32_t>(real_greatestPriority);
    return ret;
}

// ── cuCtxGetCacheConfig (server handler) ──
// cuCtxGetCacheConfig → real CUDA call on GPU
static CtxGetCacheConfigRet handle_ctx_get_cache_config() {
    unsigned int real_config = 0;
    CUresult res = cuCtxGetCacheConfig(&real_config);
    CtxGetCacheConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.config = static_cast<uint32_t>(real_config);
    return ret;
}

// ── cuCtxSetCacheConfig (server handler) ──
// cuCtxSetCacheConfig → real CUDA call on GPU
static CtxSetCacheConfigRet handle_ctx_set_cache_config(uint32_t config) {
    CUresult res = cuCtxSetCacheConfig(static_cast<unsigned int>(config));
    CtxSetCacheConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxGetSharedMemConfig (server handler) ──
// cuCtxGetSharedMemConfig → real CUDA call on GPU
static CtxGetSharedMemConfigRet handle_ctx_get_shared_mem_config() {
    unsigned int real_pConfig = 0;
    CUresult res = cuCtxGetSharedMemConfig(&real_pConfig);
    CtxGetSharedMemConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pConfig = static_cast<uint32_t>(real_pConfig);
    return ret;
}

// ── cuCtxSetSharedMemConfig (server handler) ──
// cuCtxSetSharedMemConfig → real CUDA call on GPU
static CtxSetSharedMemConfigRet handle_ctx_set_shared_mem_config(uint32_t config) {
    CUresult res = cuCtxSetSharedMemConfig(static_cast<unsigned int>(config));
    CtxSetSharedMemConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxGetApiVersion (server handler) ──
// cuCtxGetApiVersion → real CUDA call on GPU
static CtxGetApiVersionRet handle_ctx_get_api_version(uint64_t ctx) {
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    unsigned int real_apiVersion = 0;
    CUresult res = cuCtxGetApiVersion(real_ctx, &real_apiVersion);
    CtxGetApiVersionRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.apiVersion = static_cast<uint32_t>(real_apiVersion);
    return ret;
}

// ── cuCtxGetLimit (server handler) ──
// cuCtxGetLimit → real CUDA call on GPU
static CtxGetLimitRet handle_ctx_get_limit(uint32_t limit) {
    size_t real_pvalue = 0;
    CUresult res = cuCtxGetLimit(&real_pvalue, static_cast<unsigned int>(limit));
    CtxGetLimitRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pvalue = static_cast<uint64_t>(real_pvalue);
    return ret;
}

// ── cuCtxSetLimit (server handler) ──
// cuCtxSetLimit → real CUDA call on GPU
static CtxSetLimitRet handle_ctx_set_limit(uint32_t limit, uint64_t value) {
    CUresult res = cuCtxSetLimit(static_cast<unsigned int>(limit), static_cast<size_t>(value));
    CtxSetLimitRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxEnablePeerAccess (server handler) ──
// cuCtxEnablePeerAccess → real CUDA call on GPU
static CtxEnablePeerAccessRet handle_ctx_enable_peer_access(uint64_t peerContext, uint32_t Flags) {
    CUcontext real_peerContext = reinterpret_cast<CUcontext>(g_vhandles.translate(peerContext));
    CUresult res = cuCtxEnablePeerAccess(real_peerContext, static_cast<unsigned int>(Flags));
    CtxEnablePeerAccessRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxDisablePeerAccess (server handler) ──
// cuCtxDisablePeerAccess → real CUDA call on GPU
static CtxDisablePeerAccessRet handle_ctx_disable_peer_access(uint64_t peerContext) {
    CUcontext real_peerContext = reinterpret_cast<CUcontext>(g_vhandles.translate(peerContext));
    CUresult res = cuCtxDisablePeerAccess(real_peerContext);
    CtxDisablePeerAccessRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemAlloc (server handler) ──
// cuMemAlloc → real CUDA call on GPU
static MemAllocRet handle_mem_alloc(uint64_t bytesize) {
    CUdeviceptr real_dptr = 0;
    CUresult res = cuMemAlloc(&real_dptr, static_cast<size_t>(bytesize));
    MemAllocRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemAlloc_v2 (server handler) ──
// cuMemAlloc_v2 → real CUDA call on GPU
static MemAllocV2Ret handle_mem_alloc_v2(uint64_t bytesize) {
    CUdeviceptr real_dptr = 0;
    CUresult res = cuMemAlloc_v2(&real_dptr, static_cast<size_t>(bytesize));
    MemAllocV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemAllocManaged (server handler) ──
// cuMemAllocManaged → real CUDA call on GPU
static MemAllocManagedRet handle_mem_alloc_managed(uint64_t bytesize, uint32_t flags) {
    CUdeviceptr real_dptr = 0;
    CUresult res = cuMemAllocManaged(&real_dptr, static_cast<size_t>(bytesize), static_cast<unsigned int>(flags));
    MemAllocManagedRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemAllocManaged_v2 (server handler) ──
// cuMemAllocManaged_v2 → real CUDA call on GPU
static MemAllocManagedV2Ret handle_mem_alloc_managed_v2(uint64_t bytesize, uint32_t flags) {
    CUdeviceptr real_dptr = 0;
    CUresult res = cuMemAllocManaged_v2(&real_dptr, static_cast<size_t>(bytesize), static_cast<unsigned int>(flags));
    MemAllocManagedV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemFree (server handler) ──
// cuMemFree → real CUDA call on GPU
static MemFreeRet handle_mem_free(uint64_t dptr) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuMemFree(real_dptr);
    MemFreeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemFree_v2 (server handler) ──
// cuMemFree_v2 → real CUDA call on GPU
static MemFreeV2Ret handle_mem_free_v2(uint64_t dptr) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuMemFree_v2(real_dptr);
    MemFreeV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemFreeHost (server handler) ──
// cuMemFreeHost → real CUDA call on GPU
static MemFreeHostRet handle_mem_free_host(uint64_t p) {
    CUresult res = cuMemFreeHost(p);
    MemFreeHostRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemHostAlloc (server handler) ──
// cuMemHostAlloc → real CUDA call on GPU
static MemHostAllocRet handle_mem_host_alloc(uint64_t bytesize, uint32_t Flags) {
    CUresult res = cuMemHostAlloc(&real_pp, static_cast<size_t>(bytesize), static_cast<unsigned int>(Flags));
    MemHostAllocRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemHostRegister (server handler) ──
// cuMemHostRegister → real CUDA call on GPU
static MemHostRegisterRet handle_mem_host_register(uint64_t p, uint64_t bytesize, uint32_t Flags) {
    CUresult res = cuMemHostRegister(p, static_cast<size_t>(bytesize), static_cast<unsigned int>(Flags));
    MemHostRegisterRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemHostRegister_v2 (server handler) ──
// cuMemHostRegister_v2 → real CUDA call on GPU
static MemHostRegisterV2Ret handle_mem_host_register_v2(uint64_t p, uint64_t bytesize, uint32_t Flags) {
    CUresult res = cuMemHostRegister_v2(p, static_cast<size_t>(bytesize), static_cast<unsigned int>(Flags));
    MemHostRegisterV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemHostUnregister (server handler) ──
// cuMemHostUnregister → real CUDA call on GPU
static MemHostUnregisterRet handle_mem_host_unregister(uint64_t p) {
    CUresult res = cuMemHostUnregister(p);
    MemHostUnregisterRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemHostGetDevicePointer (server handler) ──
// cuMemHostGetDevicePointer → real CUDA call on GPU
static MemHostGetDevicePointerRet handle_mem_host_get_device_pointer(uint64_t p, uint32_t Flags) {
    CUdeviceptr real_pdptr = 0;
    CUresult res = cuMemHostGetDevicePointer(&real_pdptr, p, static_cast<unsigned int>(Flags));
    MemHostGetDevicePointerRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pdptr = static_cast<uint64_t>(real_pdptr);
    if (res == CUDA_SUCCESS && real_pdptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemHostGetDevicePointer_v2 (server handler) ──
// cuMemHostGetDevicePointer_v2 → real CUDA call on GPU
static MemHostGetDevicePointerV2Ret handle_mem_host_get_device_pointer_v2(uint64_t p, uint32_t Flags) {
    CUdeviceptr real_pdptr = 0;
    CUresult res = cuMemHostGetDevicePointer_v2(&real_pdptr, p, static_cast<unsigned int>(Flags));
    MemHostGetDevicePointerV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.pdptr = static_cast<uint64_t>(real_pdptr);
    if (res == CUDA_SUCCESS && real_pdptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemHostGetFlags (server handler) ──
// cuMemHostGetFlags → real CUDA call on GPU
static MemHostGetFlagsRet handle_mem_host_get_flags(uint64_t p) {
    unsigned int real_pFlags = 0;
    CUresult res = cuMemHostGetFlags(&real_pFlags, p);
    MemHostGetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pFlags = static_cast<uint32_t>(real_pFlags);
    return ret;
}

// ── cuMemGetInfo (server handler) ──
// cuMemGetInfo → real CUDA call on GPU
static MemGetInfoRet handle_mem_get_info() {
    size_t real_free = 0;
    size_t real_total = 0;
    CUresult res = cuMemGetInfo(&real_free, &real_total);
    MemGetInfoRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.free = static_cast<uint64_t>(real_free);
    ret.total = static_cast<uint64_t>(real_total);
    return ret;
}

// ── cuMemGetInfo_v2 (server handler) ──
// cuMemGetInfo_v2 → real CUDA call on GPU
static MemGetInfoV2Ret handle_mem_get_info_v2() {
    size_t real_free = 0;
    size_t real_total = 0;
    CUresult res = cuMemGetInfo_v2(&real_free, &real_total);
    MemGetInfoV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.free = static_cast<uint64_t>(real_free);
    ret.total = static_cast<uint64_t>(real_total);
    return ret;
}

// ── cuMemAllocPitch (server handler) ──
// cuMemAllocPitch → real CUDA call on GPU
static MemAllocPitchRet handle_mem_alloc_pitch(uint64_t WidthInBytes, uint64_t Height, uint32_t ElementSizeBytes) {
    CUdeviceptr real_dptr = 0;
    size_t real_pPitch = 0;
    CUresult res = cuMemAllocPitch(&real_dptr, &real_pPitch, static_cast<size_t>(WidthInBytes), static_cast<size_t>(Height), static_cast<unsigned int>(ElementSizeBytes));
    MemAllocPitchRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    ret.pPitch = static_cast<uint64_t>(real_pPitch);
    return ret;
}

// ── cuMemAllocPitch_v2 (server handler) ──
// cuMemAllocPitch_v2 → real CUDA call on GPU
static MemAllocPitchV2Ret handle_mem_alloc_pitch_v2(uint64_t WidthInBytes, uint64_t Height, uint32_t ElementSizeBytes) {
    CUdeviceptr real_dptr = 0;
    size_t real_pPitch = 0;
    CUresult res = cuMemAllocPitch_v2(&real_dptr, &real_pPitch, static_cast<size_t>(WidthInBytes), static_cast<size_t>(Height), static_cast<unsigned int>(ElementSizeBytes));
    MemAllocPitchV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    ret.pPitch = static_cast<uint64_t>(real_pPitch);
    return ret;
}

// ── cuMemcpyHtoD (server handler) ──
// cuMemcpyHtoD → real CUDA call on GPU
static MemcpyHtoDRet handle_memcpy_hto_d(uint64_t dstDevice, uint64_t srcHost, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemcpyHtoD(real_dstDevice, srcHost, static_cast<size_t>(ByteCount));
    MemcpyHtoDRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyHtoD_v2 (server handler) ──
// cuMemcpyHtoD_v2 → real CUDA call on GPU
static MemcpyHtoDV2Ret handle_memcpy_hto_d_v2(uint64_t dstDevice, uint64_t srcHost, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemcpyHtoD_v2(real_dstDevice, srcHost, static_cast<size_t>(ByteCount));
    MemcpyHtoDV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoH (server handler) ──
// cuMemcpyDtoH → real CUDA call on GPU
static MemcpyDtoHRet handle_memcpy_dto_h(uint64_t srcDevice, uint64_t ByteCount) {
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUresult res = cuMemcpyDtoH(&real_dstHost, real_srcDevice, static_cast<size_t>(ByteCount));
    MemcpyDtoHRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoH_v2 (server handler) ──
// cuMemcpyDtoH_v2 → real CUDA call on GPU
static MemcpyDtoHV2Ret handle_memcpy_dto_h_v2(uint64_t srcDevice, uint64_t ByteCount) {
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUresult res = cuMemcpyDtoH_v2(&real_dstHost, real_srcDevice, static_cast<size_t>(ByteCount));
    MemcpyDtoHV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoD (server handler) ──
// cuMemcpyDtoD → real CUDA call on GPU
static MemcpyDtoDRet handle_memcpy_dto_d(uint64_t dstDevice, uint64_t srcDevice, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUresult res = cuMemcpyDtoD(real_dstDevice, real_srcDevice, static_cast<size_t>(ByteCount));
    MemcpyDtoDRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoD_v2 (server handler) ──
// cuMemcpyDtoD_v2 → real CUDA call on GPU
static MemcpyDtoDV2Ret handle_memcpy_dto_d_v2(uint64_t dstDevice, uint64_t srcDevice, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUresult res = cuMemcpyDtoD_v2(real_dstDevice, real_srcDevice, static_cast<size_t>(ByteCount));
    MemcpyDtoDV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyHtoDAsync (server handler) ──
// cuMemcpyHtoDAsync → real CUDA call on GPU
static MemcpyHtoDAsyncRet handle_memcpy_hto_d_async(uint64_t dstDevice, uint64_t srcHost, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyHtoDAsync(real_dstDevice, srcHost, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyHtoDAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyHtoDAsync_v2 (server handler) ──
// cuMemcpyHtoDAsync_v2 → real CUDA call on GPU
static MemcpyHtoDAsyncV2Ret handle_memcpy_hto_d_async_v2(uint64_t dstDevice, uint64_t srcHost, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyHtoDAsync_v2(real_dstDevice, srcHost, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyHtoDAsyncV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoHAsync (server handler) ──
// cuMemcpyDtoHAsync → real CUDA call on GPU
static MemcpyDtoHAsyncRet handle_memcpy_dto_h_async(uint64_t srcDevice, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyDtoHAsync(&real_dstHost, real_srcDevice, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyDtoHAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoHAsync_v2 (server handler) ──
// cuMemcpyDtoHAsync_v2 → real CUDA call on GPU
static MemcpyDtoHAsyncV2Ret handle_memcpy_dto_h_async_v2(uint64_t srcDevice, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyDtoHAsync_v2(&real_dstHost, real_srcDevice, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyDtoHAsyncV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoDAsync (server handler) ──
// cuMemcpyDtoDAsync → real CUDA call on GPU
static MemcpyDtoDAsyncRet handle_memcpy_dto_d_async(uint64_t dstDevice, uint64_t srcDevice, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyDtoDAsync(real_dstDevice, real_srcDevice, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyDtoDAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoDAsync_v2 (server handler) ──
// cuMemcpyDtoDAsync_v2 → real CUDA call on GPU
static MemcpyDtoDAsyncV2Ret handle_memcpy_dto_d_async_v2(uint64_t dstDevice, uint64_t srcDevice, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyDtoDAsync_v2(real_dstDevice, real_srcDevice, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyDtoDAsyncV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpy (server handler) ──
// cuMemcpy → real CUDA call on GPU
static MemcpyRet handle_memcpy(uint64_t dst, uint64_t src, uint64_t ByteCount) {
    CUdeviceptr real_dst = static_cast<CUdeviceptr>(g_vhandles.translate(dst));
    CUdeviceptr real_src = static_cast<CUdeviceptr>(g_vhandles.translate(src));
    CUresult res = cuMemcpy(real_dst, real_src, static_cast<size_t>(ByteCount));
    MemcpyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpy_v2 (server handler) ──
// cuMemcpy_v2 → real CUDA call on GPU
static MemcpyV2Ret handle_memcpy_v2(uint64_t dst, uint64_t src, uint64_t ByteCount) {
    CUdeviceptr real_dst = static_cast<CUdeviceptr>(g_vhandles.translate(dst));
    CUdeviceptr real_src = static_cast<CUdeviceptr>(g_vhandles.translate(src));
    CUresult res = cuMemcpy_v2(real_dst, real_src, static_cast<size_t>(ByteCount));
    MemcpyV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyAsync (server handler) ──
// cuMemcpyAsync → real CUDA call on GPU
static MemcpyAsyncRet handle_memcpy_async(uint64_t dst, uint64_t src, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dst = static_cast<CUdeviceptr>(g_vhandles.translate(dst));
    CUdeviceptr real_src = static_cast<CUdeviceptr>(g_vhandles.translate(src));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyAsync(real_dst, real_src, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyPeer (server handler) ──
// cuMemcpyPeer → real CUDA call on GPU
static MemcpyPeerRet handle_memcpy_peer(uint64_t dstDevice, uint64_t dstContext, uint64_t srcDevice, uint64_t srcContext, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUcontext real_dstContext = reinterpret_cast<CUcontext>(g_vhandles.translate(dstContext));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUcontext real_srcContext = reinterpret_cast<CUcontext>(g_vhandles.translate(srcContext));
    CUresult res = cuMemcpyPeer(real_dstDevice, real_dstContext, real_srcDevice, real_srcContext, static_cast<size_t>(ByteCount));
    MemcpyPeerRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyPeer_v2 (server handler) ──
// cuMemcpyPeer_v2 → real CUDA call on GPU
static MemcpyPeerV2Ret handle_memcpy_peer_v2(uint64_t dstDevice, uint64_t dstContext, uint64_t srcDevice, uint64_t srcContext, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUcontext real_dstContext = reinterpret_cast<CUcontext>(g_vhandles.translate(dstContext));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUcontext real_srcContext = reinterpret_cast<CUcontext>(g_vhandles.translate(srcContext));
    CUresult res = cuMemcpyPeer_v2(real_dstDevice, real_dstContext, real_srcDevice, real_srcContext, static_cast<size_t>(ByteCount));
    MemcpyPeerV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyPeerAsync (server handler) ──
// cuMemcpyPeerAsync → real CUDA call on GPU
static MemcpyPeerAsyncRet handle_memcpy_peer_async(uint64_t dstDevice, uint64_t dstContext, uint64_t srcDevice, uint64_t srcContext, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUcontext real_dstContext = reinterpret_cast<CUcontext>(g_vhandles.translate(dstContext));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUcontext real_srcContext = reinterpret_cast<CUcontext>(g_vhandles.translate(srcContext));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyPeerAsync(real_dstDevice, real_dstContext, real_srcDevice, real_srcContext, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyPeerAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyPeerAsync_v2 (server handler) ──
// cuMemcpyPeerAsync_v2 → real CUDA call on GPU
static MemcpyPeerAsyncV2Ret handle_memcpy_peer_async_v2(uint64_t dstDevice, uint64_t dstContext, uint64_t srcDevice, uint64_t srcContext, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUcontext real_dstContext = reinterpret_cast<CUcontext>(g_vhandles.translate(dstContext));
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUcontext real_srcContext = reinterpret_cast<CUcontext>(g_vhandles.translate(srcContext));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyPeerAsync_v2(real_dstDevice, real_dstContext, real_srcDevice, real_srcContext, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyPeerAsyncV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD8 (server handler) ──
// cuMemsetD8 → real CUDA call on GPU
static MemsetD8Ret handle_memset_d8(uint64_t dstDevice, uint32_t uc, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD8(real_dstDevice, static_cast<unsigned int>(uc), static_cast<size_t>(N));
    MemsetD8Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD8_v2 (server handler) ──
// cuMemsetD8_v2 → real CUDA call on GPU
static MemsetD8V2Ret handle_memset_d8_v2(uint64_t dstDevice, uint32_t uc, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD8_v2(real_dstDevice, static_cast<unsigned int>(uc), static_cast<size_t>(N));
    MemsetD8V2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD16 (server handler) ──
// cuMemsetD16 → real CUDA call on GPU
static MemsetD16Ret handle_memset_d16(uint64_t dstDevice, uint32_t us, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD16(real_dstDevice, static_cast<unsigned int>(us), static_cast<size_t>(N));
    MemsetD16Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD16_v2 (server handler) ──
// cuMemsetD16_v2 → real CUDA call on GPU
static MemsetD16V2Ret handle_memset_d16_v2(uint64_t dstDevice, uint32_t us, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD16_v2(real_dstDevice, static_cast<unsigned int>(us), static_cast<size_t>(N));
    MemsetD16V2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD32 (server handler) ──
// cuMemsetD32 → real CUDA call on GPU
static MemsetD32Ret handle_memset_d32(uint64_t dstDevice, uint32_t ui, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD32(real_dstDevice, static_cast<unsigned int>(ui), static_cast<size_t>(N));
    MemsetD32Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD32_v2 (server handler) ──
// cuMemsetD32_v2 → real CUDA call on GPU
static MemsetD32V2Ret handle_memset_d32_v2(uint64_t dstDevice, uint32_t ui, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD32_v2(real_dstDevice, static_cast<unsigned int>(ui), static_cast<size_t>(N));
    MemsetD32V2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D8 (server handler) ──
// cuMemsetD2D8 → real CUDA call on GPU
static MemsetD2D8Ret handle_memset_d2_d8(uint64_t dstDevice, uint64_t dstPitch, uint32_t uc, uint64_t Width, uint64_t Height) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD2D8(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(uc), static_cast<size_t>(Width), static_cast<size_t>(Height));
    MemsetD2D8Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D8_v2 (server handler) ──
// cuMemsetD2D8_v2 → real CUDA call on GPU
static MemsetD2D8V2Ret handle_memset_d2_d8_v2(uint64_t dstDevice, uint64_t dstPitch, uint32_t uc, uint64_t Width, uint64_t Height) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD2D8_v2(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(uc), static_cast<size_t>(Width), static_cast<size_t>(Height));
    MemsetD2D8V2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D16 (server handler) ──
// cuMemsetD2D16 → real CUDA call on GPU
static MemsetD2D16Ret handle_memset_d2_d16(uint64_t dstDevice, uint64_t dstPitch, uint32_t us, uint64_t Width, uint64_t Height) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD2D16(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(us), static_cast<size_t>(Width), static_cast<size_t>(Height));
    MemsetD2D16Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D16_v2 (server handler) ──
// cuMemsetD2D16_v2 → real CUDA call on GPU
static MemsetD2D16V2Ret handle_memset_d2_d16_v2(uint64_t dstDevice, uint64_t dstPitch, uint32_t us, uint64_t Width, uint64_t Height) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD2D16_v2(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(us), static_cast<size_t>(Width), static_cast<size_t>(Height));
    MemsetD2D16V2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D32 (server handler) ──
// cuMemsetD2D32 → real CUDA call on GPU
static MemsetD2D32Ret handle_memset_d2_d32(uint64_t dstDevice, uint64_t dstPitch, uint32_t ui, uint64_t Width, uint64_t Height) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD2D32(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(ui), static_cast<size_t>(Width), static_cast<size_t>(Height));
    MemsetD2D32Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D32_v2 (server handler) ──
// cuMemsetD2D32_v2 → real CUDA call on GPU
static MemsetD2D32V2Ret handle_memset_d2_d32_v2(uint64_t dstDevice, uint64_t dstPitch, uint32_t ui, uint64_t Width, uint64_t Height) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD2D32_v2(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(ui), static_cast<size_t>(Width), static_cast<size_t>(Height));
    MemsetD2D32V2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD8Async (server handler) ──
// cuMemsetD8Async → real CUDA call on GPU
static MemsetD8AsyncRet handle_memset_d8_async(uint64_t dstDevice, uint32_t uc, uint64_t N, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD8Async(real_dstDevice, static_cast<unsigned int>(uc), static_cast<size_t>(N), real_hStream);
    MemsetD8AsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD16Async (server handler) ──
// cuMemsetD16Async → real CUDA call on GPU
static MemsetD16AsyncRet handle_memset_d16_async(uint64_t dstDevice, uint32_t us, uint64_t N, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD16Async(real_dstDevice, static_cast<unsigned int>(us), static_cast<size_t>(N), real_hStream);
    MemsetD16AsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD32Async (server handler) ──
// cuMemsetD32Async → real CUDA call on GPU
static MemsetD32AsyncRet handle_memset_d32_async(uint64_t dstDevice, uint32_t ui, uint64_t N, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD32Async(real_dstDevice, static_cast<unsigned int>(ui), static_cast<size_t>(N), real_hStream);
    MemsetD32AsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D8Async (server handler) ──
// cuMemsetD2D8Async → real CUDA call on GPU
static MemsetD2D8AsyncRet handle_memset_d2_d8_async(uint64_t dstDevice, uint64_t dstPitch, uint32_t uc, uint64_t Width, uint64_t Height, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD2D8Async(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(uc), static_cast<size_t>(Width), static_cast<size_t>(Height), real_hStream);
    MemsetD2D8AsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D16Async (server handler) ──
// cuMemsetD2D16Async → real CUDA call on GPU
static MemsetD2D16AsyncRet handle_memset_d2_d16_async(uint64_t dstDevice, uint64_t dstPitch, uint32_t us, uint64_t Width, uint64_t Height, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD2D16Async(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(us), static_cast<size_t>(Width), static_cast<size_t>(Height), real_hStream);
    MemsetD2D16AsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD2D32Async (server handler) ──
// cuMemsetD2D32Async → real CUDA call on GPU
static MemsetD2D32AsyncRet handle_memset_d2_d32_async(uint64_t dstDevice, uint64_t dstPitch, uint32_t ui, uint64_t Width, uint64_t Height, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD2D32Async(real_dstDevice, static_cast<size_t>(dstPitch), static_cast<unsigned int>(ui), static_cast<size_t>(Width), static_cast<size_t>(Height), real_hStream);
    MemsetD2D32AsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemAddressReserve (server handler) ──
// cuMemAddressReserve → real CUDA call on GPU
static MemAddressReserveRet handle_mem_address_reserve(uint64_t size, uint64_t alignment, uint64_t addr, uint64_t flags) {
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    CUdeviceptr real_ptr = 0;
    CUresult res = cuMemAddressReserve(&real_ptr, static_cast<size_t>(size), static_cast<size_t>(alignment), real_addr, static_cast<unsigned long long>(flags));
    MemAddressReserveRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.ptr = static_cast<uint64_t>(real_ptr);
    if (res == CUDA_SUCCESS && real_ptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemAddressFree (server handler) ──
// cuMemAddressFree → real CUDA call on GPU
static MemAddressFreeRet handle_mem_address_free(uint64_t ptr, uint64_t size) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    CUresult res = cuMemAddressFree(real_ptr, static_cast<size_t>(size));
    MemAddressFreeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemCreate (server handler) ──
// cuMemCreate → real CUDA call on GPU
static MemCreateRet handle_mem_create(uint64_t size, uint64_t desc, uint64_t flags) {
    CUmemGenericAllocationHandle real_handle = nullptr;
    CUresult res = cuMemCreate(&real_handle, static_cast<size_t>(size), desc, static_cast<unsigned long long>(flags));
    MemCreateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.handle = reinterpret_cast<uint64_t>(real_handle);
    if (res == CUDA_SUCCESS && real_handle != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemRelease (server handler) ──
// cuMemRelease → real CUDA call on GPU
static MemReleaseRet handle_mem_release(uint64_t handle) {
    CUmemGenericAllocationHandle real_handle = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(handle));
    CUresult res = cuMemRelease(real_handle);
    MemReleaseRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemMap (server handler) ──
// cuMemMap → real CUDA call on GPU
static MemMapRet handle_mem_map(uint64_t ptr, uint64_t size, uint64_t offset, uint64_t handle) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    CUmemGenericAllocationHandle real_handle = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(handle));
    CUresult res = cuMemMap(real_ptr, static_cast<size_t>(size), static_cast<size_t>(offset), real_handle);
    MemMapRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemUnmap (server handler) ──
// cuMemUnmap → real CUDA call on GPU
static MemUnmapRet handle_mem_unmap(uint64_t ptr, uint64_t size) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    CUresult res = cuMemUnmap(real_ptr, static_cast<size_t>(size));
    MemUnmapRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemSetAccess (server handler) ──
// cuMemSetAccess → real CUDA call on GPU
static MemSetAccessRet handle_mem_set_access(uint64_t ptr, uint64_t size, uint64_t desc, uint64_t count) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    CUresult res = cuMemSetAccess(real_ptr, static_cast<size_t>(size), desc, static_cast<size_t>(count));
    MemSetAccessRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemGetAccess (server handler) ──
// cuMemGetAccess → real CUDA call on GPU
static MemGetAccessRet handle_mem_get_access(uint64_t location, uint64_t ptr) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    unsigned long long real_flags = 0;
    CUresult res = cuMemGetAccess(&real_flags, location, real_ptr);
    MemGetAccessRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.flags = static_cast<uint64_t>(real_flags);
    return ret;
}

// ── cuMemGetAllocationGranularity (server handler) ──
// cuMemGetAllocationGranularity → real CUDA call on GPU
static MemGetAllocationGranularityRet handle_mem_get_allocation_granularity(uint64_t prop, uint64_t flags) {
    size_t real_granularity = 0;
    CUresult res = cuMemGetAllocationGranularity(&real_granularity, prop, static_cast<unsigned long long>(flags));
    MemGetAllocationGranularityRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.granularity = static_cast<uint64_t>(real_granularity);
    return ret;
}

// ── cuMemRetainAllocationHandle (server handler) ──
// cuMemRetainAllocationHandle → real CUDA call on GPU
static MemRetainAllocationHandleRet handle_mem_retain_allocation_handle(uint64_t addr) {
    CUmemGenericAllocationHandle real_handle = nullptr;
    CUresult res = cuMemRetainAllocationHandle(&real_handle, addr);
    MemRetainAllocationHandleRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.handle = reinterpret_cast<uint64_t>(real_handle);
    if (res == CUDA_SUCCESS && real_handle != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleLoad (server handler) ──
// cuModuleLoad → real CUDA call on GPU
static ModuleLoadRet handle_module_load(std::string fname) {
    CUmodule real_module = nullptr;
    CUresult res = cuModuleLoad(&real_module, fname.c_str());
    ModuleLoadRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.module = reinterpret_cast<uint64_t>(real_module);
    if (res == CUDA_SUCCESS && real_module != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleLoadData (server handler) ──
// cuModuleLoadData → real CUDA call on GPU
static ModuleLoadDataRet handle_module_load_data(uint64_t image) {
    CUmodule real_module = nullptr;
    CUresult res = cuModuleLoadData(&real_module, image);
    ModuleLoadDataRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.module = reinterpret_cast<uint64_t>(real_module);
    if (res == CUDA_SUCCESS && real_module != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleLoadDataEx (server handler) ──
// cuModuleLoadDataEx → real CUDA call on GPU
static ModuleLoadDataExRet handle_module_load_data_ex(uint64_t image, uint32_t numOptions, uint64_t options, uint64_t optionValues) {
    CUmodule real_module = nullptr;
    CUresult res = cuModuleLoadDataEx(&real_module, image, static_cast<unsigned int>(numOptions), options, optionValues);
    ModuleLoadDataExRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.module = reinterpret_cast<uint64_t>(real_module);
    if (res == CUDA_SUCCESS && real_module != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleLoadFatBinary (server handler) ──
// cuModuleLoadFatBinary → real CUDA call on GPU
static ModuleLoadFatBinaryRet handle_module_load_fat_binary(uint64_t fatCubin) {
    CUmodule real_module = nullptr;
    CUresult res = cuModuleLoadFatBinary(&real_module, fatCubin);
    ModuleLoadFatBinaryRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.module = reinterpret_cast<uint64_t>(real_module);
    if (res == CUDA_SUCCESS && real_module != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleUnload (server handler) ──
// cuModuleUnload → real CUDA call on GPU
static ModuleUnloadRet handle_module_unload(uint64_t hmod) {
    CUmodule real_hmod = reinterpret_cast<CUmodule>(g_vhandles.translate(hmod));
    CUresult res = cuModuleUnload(real_hmod);
    ModuleUnloadRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuModuleGetFunction (server handler) ──
// cuModuleGetFunction → real CUDA call on GPU
static ModuleGetFunctionRet handle_module_get_function(uint64_t hmod, std::string name) {
    CUmodule real_hmod = reinterpret_cast<CUmodule>(g_vhandles.translate(hmod));
    CUfunction real_hfunc = nullptr;
    CUresult res = cuModuleGetFunction(&real_hfunc, real_hmod, name.c_str());
    ModuleGetFunctionRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.hfunc = reinterpret_cast<uint64_t>(real_hfunc);
    if (res == CUDA_SUCCESS && real_hfunc != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleGetGlobal (server handler) ──
// cuModuleGetGlobal → real CUDA call on GPU
static ModuleGetGlobalRet handle_module_get_global(uint64_t hmod, std::string name) {
    CUmodule real_hmod = reinterpret_cast<CUmodule>(g_vhandles.translate(hmod));
    CUdeviceptr real_dptr = 0;
    size_t real_bytes = 0;
    CUresult res = cuModuleGetGlobal(&real_dptr, &real_bytes, real_hmod, name.c_str());
    ModuleGetGlobalRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    ret.bytes = static_cast<uint64_t>(real_bytes);
    return ret;
}

// ── cuModuleGetGlobal_v2 (server handler) ──
// cuModuleGetGlobal_v2 → real CUDA call on GPU
static ModuleGetGlobalV2Ret handle_module_get_global_v2(uint64_t hmod, std::string name) {
    CUmodule real_hmod = reinterpret_cast<CUmodule>(g_vhandles.translate(hmod));
    CUdeviceptr real_dptr = 0;
    size_t real_bytes = 0;
    CUresult res = cuModuleGetGlobal_v2(&real_dptr, &real_bytes, real_hmod, name.c_str());
    ModuleGetGlobalV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    ret.bytes = static_cast<uint64_t>(real_bytes);
    return ret;
}

// ── cuModuleGetTexRef (server handler) ──
// cuModuleGetTexRef → real CUDA call on GPU
static ModuleGetTexRefRet handle_module_get_tex_ref(uint64_t hmod, std::string name) {
    CUmodule real_hmod = reinterpret_cast<CUmodule>(g_vhandles.translate(hmod));
    CUtexref real_pTexRef = nullptr;
    CUresult res = cuModuleGetTexRef(&real_pTexRef, real_hmod, name.c_str());
    ModuleGetTexRefRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pTexRef = reinterpret_cast<uint64_t>(real_pTexRef);
    if (res == CUDA_SUCCESS && real_pTexRef != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuModuleGetSurfRef (server handler) ──
// cuModuleGetSurfRef → real CUDA call on GPU
static ModuleGetSurfRefRet handle_module_get_surf_ref(uint64_t hmod, std::string name) {
    CUmodule real_hmod = reinterpret_cast<CUmodule>(g_vhandles.translate(hmod));
    CUsurfref real_pSurfRef = nullptr;
    CUresult res = cuModuleGetSurfRef(&real_pSurfRef, real_hmod, name.c_str());
    ModuleGetSurfRefRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pSurfRef = reinterpret_cast<uint64_t>(real_pSurfRef);
    if (res == CUDA_SUCCESS && real_pSurfRef != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuStreamCreate (server handler) ──
// cuStreamCreate → real CUDA call on GPU
static StreamCreateRet handle_stream_create(uint32_t Flags) {
    CUstream real_phStream = nullptr;
    CUresult res = cuStreamCreate(&real_phStream, static_cast<unsigned int>(Flags));
    StreamCreateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phStream = reinterpret_cast<uint64_t>(real_phStream);
    if (res == CUDA_SUCCESS && real_phStream != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuStreamCreateWithPriority (server handler) ──
// cuStreamCreateWithPriority → real CUDA call on GPU
static StreamCreateWithPriorityRet handle_stream_create_with_priority(uint32_t flags, int32_t priority) {
    CUstream real_phStream = nullptr;
    CUresult res = cuStreamCreateWithPriority(&real_phStream, static_cast<unsigned int>(flags), priority);
    StreamCreateWithPriorityRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phStream = reinterpret_cast<uint64_t>(real_phStream);
    if (res == CUDA_SUCCESS && real_phStream != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuStreamDestroy (server handler) ──
// cuStreamDestroy → real CUDA call on GPU
static StreamDestroyRet handle_stream_destroy(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamDestroy(real_hStream);
    StreamDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamDestroy_v2 (server handler) ──
// cuStreamDestroy_v2 → real CUDA call on GPU
static StreamDestroyV2Ret handle_stream_destroy_v2(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamDestroy_v2(real_hStream);
    StreamDestroyV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamSynchronize (server handler) ──
// cuStreamSynchronize → real CUDA call on GPU
static StreamSynchronizeRet handle_stream_synchronize(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamSynchronize(real_hStream);
    StreamSynchronizeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamGetPriority (server handler) ──
// cuStreamGetPriority → real CUDA call on GPU
static StreamGetPriorityRet handle_stream_get_priority(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    int real_priority = 0;
    CUresult res = cuStreamGetPriority(real_hStream, &real_priority);
    StreamGetPriorityRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.priority = static_cast<int32_t>(real_priority);
    return ret;
}

// ── cuStreamGetFlags (server handler) ──
// cuStreamGetFlags → real CUDA call on GPU
static StreamGetFlagsRet handle_stream_get_flags(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    unsigned int real_flags = 0;
    CUresult res = cuStreamGetFlags(real_hStream, &real_flags);
    StreamGetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.flags = static_cast<uint32_t>(real_flags);
    return ret;
}

// ── cuStreamGetCtx (server handler) ──
// cuStreamGetCtx → real CUDA call on GPU
static StreamGetCtxRet handle_stream_get_ctx(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUcontext real_pctx = nullptr;
    CUresult res = cuStreamGetCtx(real_hStream, &real_pctx);
    StreamGetCtxRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pctx = reinterpret_cast<uint64_t>(real_pctx);
    if (res == CUDA_SUCCESS && real_pctx != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuStreamWaitEvent (server handler) ──
// cuStreamWaitEvent → real CUDA call on GPU
static StreamWaitEventRet handle_stream_wait_event(uint64_t hStream, uint64_t hEvent, uint32_t Flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuStreamWaitEvent(real_hStream, real_hEvent, static_cast<unsigned int>(Flags));
    StreamWaitEventRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamWaitEvent_ptsz (server handler) ──
// cuStreamWaitEvent_ptsz → real CUDA call on GPU
static StreamWaitEventPtszRet handle_stream_wait_event_ptsz(uint64_t hStream, uint64_t hEvent, uint32_t Flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuStreamWaitEvent_ptsz(real_hStream, real_hEvent, static_cast<unsigned int>(Flags));
    StreamWaitEventPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamBeginCapture (server handler) ──
// cuStreamBeginCapture → real CUDA call on GPU
static StreamBeginCaptureRet handle_stream_begin_capture(uint64_t hStream, uint32_t mode) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamBeginCapture(real_hStream, static_cast<unsigned int>(mode));
    StreamBeginCaptureRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamBeginCapture_ptsz (server handler) ──
// cuStreamBeginCapture_ptsz → real CUDA call on GPU
static StreamBeginCapturePtszRet handle_stream_begin_capture_ptsz(uint64_t hStream, uint32_t mode) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamBeginCapture_ptsz(real_hStream, static_cast<unsigned int>(mode));
    StreamBeginCapturePtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamEndCapture (server handler) ──
// cuStreamEndCapture → real CUDA call on GPU
static StreamEndCaptureRet handle_stream_end_capture(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUgraph real_phGraph = nullptr;
    CUresult res = cuStreamEndCapture(real_hStream, &real_phGraph);
    StreamEndCaptureRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraph = reinterpret_cast<uint64_t>(real_phGraph);
    if (res == CUDA_SUCCESS && real_phGraph != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuStreamEndCapture_ptsz (server handler) ──
// cuStreamEndCapture_ptsz → real CUDA call on GPU
static StreamEndCapturePtszRet handle_stream_end_capture_ptsz(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUgraph real_phGraph = nullptr;
    CUresult res = cuStreamEndCapture_ptsz(real_hStream, &real_phGraph);
    StreamEndCapturePtszRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraph = reinterpret_cast<uint64_t>(real_phGraph);
    if (res == CUDA_SUCCESS && real_phGraph != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuStreamIsCapturing (server handler) ──
// cuStreamIsCapturing → real CUDA call on GPU
static StreamIsCapturingRet handle_stream_is_capturing(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    unsigned int real_pCaptureStatus = 0;
    CUresult res = cuStreamIsCapturing(real_hStream, &real_pCaptureStatus);
    StreamIsCapturingRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pCaptureStatus = static_cast<uint32_t>(real_pCaptureStatus);
    return ret;
}

// ── cuStreamAttachMemAsync (server handler) ──
// cuStreamAttachMemAsync → real CUDA call on GPU
static StreamAttachMemAsyncRet handle_stream_attach_mem_async(uint64_t hStream, uint64_t dptr, uint64_t length, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuStreamAttachMemAsync(real_hStream, real_dptr, static_cast<size_t>(length), static_cast<unsigned int>(flags));
    StreamAttachMemAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamAttachMemAsync_ptsz (server handler) ──
// cuStreamAttachMemAsync_ptsz → real CUDA call on GPU
static StreamAttachMemAsyncPtszRet handle_stream_attach_mem_async_ptsz(uint64_t hStream, uint64_t dptr, uint64_t length, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuStreamAttachMemAsync_ptsz(real_hStream, real_dptr, static_cast<size_t>(length), static_cast<unsigned int>(flags));
    StreamAttachMemAsyncPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamQuery (server handler) ──
// cuStreamQuery → real CUDA call on GPU
static StreamQueryRet handle_stream_query(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamQuery(real_hStream);
    StreamQueryRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamQuery_ptsz (server handler) ──
// cuStreamQuery_ptsz → real CUDA call on GPU
static StreamQueryPtszRet handle_stream_query_ptsz(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamQuery_ptsz(real_hStream);
    StreamQueryPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventCreate (server handler) ──
// cuEventCreate → real CUDA call on GPU
static EventCreateRet handle_event_create(uint32_t Flags) {
    CUevent real_phEvent = nullptr;
    CUresult res = cuEventCreate(&real_phEvent, static_cast<unsigned int>(Flags));
    EventCreateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phEvent = reinterpret_cast<uint64_t>(real_phEvent);
    if (res == CUDA_SUCCESS && real_phEvent != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuEventRecord (server handler) ──
// cuEventRecord → real CUDA call on GPU
static EventRecordRet handle_event_record(uint64_t hEvent, uint64_t hStream) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuEventRecord(real_hEvent, real_hStream);
    EventRecordRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventRecord_ptsz (server handler) ──
// cuEventRecord_ptsz → real CUDA call on GPU
static EventRecordPtszRet handle_event_record_ptsz(uint64_t hEvent, uint64_t hStream) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuEventRecord_ptsz(real_hEvent, real_hStream);
    EventRecordPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventRecordWithFlags (server handler) ──
// cuEventRecordWithFlags → real CUDA call on GPU
static EventRecordWithFlagsRet handle_event_record_with_flags(uint64_t hEvent, uint32_t flags, uint64_t hStream) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuEventRecordWithFlags(real_hEvent, static_cast<unsigned int>(flags), real_hStream);
    EventRecordWithFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventRecordWithFlags_ptsz (server handler) ──
// cuEventRecordWithFlags_ptsz → real CUDA call on GPU
static EventRecordWithFlagsPtszRet handle_event_record_with_flags_ptsz(uint64_t hEvent, uint32_t flags, uint64_t hStream) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuEventRecordWithFlags_ptsz(real_hEvent, static_cast<unsigned int>(flags), real_hStream);
    EventRecordWithFlagsPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventDestroy (server handler) ──
// cuEventDestroy → real CUDA call on GPU
static EventDestroyRet handle_event_destroy(uint64_t hEvent) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuEventDestroy(real_hEvent);
    EventDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventDestroy_v2 (server handler) ──
// cuEventDestroy_v2 → real CUDA call on GPU
static EventDestroyV2Ret handle_event_destroy_v2(uint64_t hEvent) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuEventDestroy_v2(real_hEvent);
    EventDestroyV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventSynchronize (server handler) ──
// cuEventSynchronize → real CUDA call on GPU
static EventSynchronizeRet handle_event_synchronize(uint64_t hEvent) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuEventSynchronize(real_hEvent);
    EventSynchronizeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuEventElapsedTime (server handler) ──
// cuEventElapsedTime → real CUDA call on GPU
static EventElapsedTimeRet handle_event_elapsed_time(uint64_t hStart, uint64_t hEnd) {
    CUevent real_hStart = reinterpret_cast<CUevent>(g_vhandles.translate(hStart));
    CUevent real_hEnd = reinterpret_cast<CUevent>(g_vhandles.translate(hEnd));
    float real_pMilliseconds = 0;
    CUresult res = cuEventElapsedTime(&real_pMilliseconds, real_hStart, real_hEnd);
    EventElapsedTimeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pMilliseconds = real_pMilliseconds;
    return ret;
}

// ── cuEventQuery (server handler) ──
// cuEventQuery → real CUDA call on GPU
static EventQueryRet handle_event_query(uint64_t hEvent) {
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuEventQuery(real_hEvent);
    EventQueryRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetArray (server handler) ──
// cuTexRefSetArray → real CUDA call on GPU
static TexRefSetArrayRet handle_tex_ref_set_array(uint64_t hTexRef, uint64_t hArray, uint32_t Flags) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUarray real_hArray = reinterpret_cast<CUarray>(g_vhandles.translate(hArray));
    CUresult res = cuTexRefSetArray(real_hTexRef, real_hArray, static_cast<unsigned int>(Flags));
    TexRefSetArrayRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetAddress (server handler) ──
// cuTexRefSetAddress → real CUDA call on GPU
static TexRefSetAddressRet handle_tex_ref_set_address(uint64_t hTexRef, uint64_t dptr, uint64_t bytes) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    size_t real_ByteOffset = 0;
    CUresult res = cuTexRefSetAddress(&real_ByteOffset, real_hTexRef, real_dptr, static_cast<size_t>(bytes));
    TexRefSetAddressRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.ByteOffset = static_cast<uint64_t>(real_ByteOffset);
    return ret;
}

// ── cuTexRefSetAddress_v2 (server handler) ──
// cuTexRefSetAddress_v2 → real CUDA call on GPU
static TexRefSetAddressV2Ret handle_tex_ref_set_address_v2(uint64_t hTexRef, uint64_t dptr, uint64_t bytes) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    size_t real_ByteOffset = 0;
    CUresult res = cuTexRefSetAddress_v2(&real_ByteOffset, real_hTexRef, real_dptr, static_cast<size_t>(bytes));
    TexRefSetAddressV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.ByteOffset = static_cast<uint64_t>(real_ByteOffset);
    return ret;
}

// ── cuTexRefSetFormat (server handler) ──
// cuTexRefSetFormat → real CUDA call on GPU
static TexRefSetFormatRet handle_tex_ref_set_format(uint64_t hTexRef, uint32_t fmt, int32_t NumPackedComponents) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetFormat(real_hTexRef, static_cast<unsigned int>(fmt), NumPackedComponents);
    TexRefSetFormatRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetAddressMode (server handler) ──
// cuTexRefSetAddressMode → real CUDA call on GPU
static TexRefSetAddressModeRet handle_tex_ref_set_address_mode(uint64_t hTexRef, int32_t dim, uint32_t am) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetAddressMode(real_hTexRef, dim, static_cast<unsigned int>(am));
    TexRefSetAddressModeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetFilterMode (server handler) ──
// cuTexRefSetFilterMode → real CUDA call on GPU
static TexRefSetFilterModeRet handle_tex_ref_set_filter_mode(uint64_t hTexRef, uint32_t fm) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetFilterMode(real_hTexRef, static_cast<unsigned int>(fm));
    TexRefSetFilterModeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetMipmapFilterMode (server handler) ──
// cuTexRefSetMipmapFilterMode → real CUDA call on GPU
static TexRefSetMipmapFilterModeRet handle_tex_ref_set_mipmap_filter_mode(uint64_t hTexRef, uint32_t fm) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetMipmapFilterMode(real_hTexRef, static_cast<unsigned int>(fm));
    TexRefSetMipmapFilterModeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetMipmapLevelBias (server handler) ──
// cuTexRefSetMipmapLevelBias → real CUDA call on GPU
static TexRefSetMipmapLevelBiasRet handle_tex_ref_set_mipmap_level_bias(uint64_t hTexRef, float bias) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetMipmapLevelBias(real_hTexRef, bias);
    TexRefSetMipmapLevelBiasRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetMipmapLevelClamp (server handler) ──
// cuTexRefSetMipmapLevelClamp → real CUDA call on GPU
static TexRefSetMipmapLevelClampRet handle_tex_ref_set_mipmap_level_clamp(uint64_t hTexRef, float minMipmapLevelClamp, float maxMipmapLevelClamp) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetMipmapLevelClamp(real_hTexRef, minMipmapLevelClamp, maxMipmapLevelClamp);
    TexRefSetMipmapLevelClampRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetMaxAnisotropy (server handler) ──
// cuTexRefSetMaxAnisotropy → real CUDA call on GPU
static TexRefSetMaxAnisotropyRet handle_tex_ref_set_max_anisotropy(uint64_t hTexRef, uint32_t maxAniso) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetMaxAnisotropy(real_hTexRef, static_cast<unsigned int>(maxAniso));
    TexRefSetMaxAnisotropyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetBorderColor (server handler) ──
// cuTexRefSetBorderColor → real CUDA call on GPU
static TexRefSetBorderColorRet handle_tex_ref_set_border_color(uint64_t hTexRef, uint64_t pBorderColor) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetBorderColor(real_hTexRef, pBorderColor);
    TexRefSetBorderColorRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefSetFlags (server handler) ──
// cuTexRefSetFlags → real CUDA call on GPU
static TexRefSetFlagsRet handle_tex_ref_set_flags(uint64_t hTexRef, uint32_t Flags) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefSetFlags(real_hTexRef, static_cast<unsigned int>(Flags));
    TexRefSetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefGetArray (server handler) ──
// cuTexRefGetArray → real CUDA call on GPU
static TexRefGetArrayRet handle_tex_ref_get_array(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUarray real_phArray = nullptr;
    CUresult res = cuTexRefGetArray(&real_phArray, real_hTexRef);
    TexRefGetArrayRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phArray = reinterpret_cast<uint64_t>(real_phArray);
    if (res == CUDA_SUCCESS && real_phArray != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuTexRefGetAddress (server handler) ──
// cuTexRefGetAddress → real CUDA call on GPU
static TexRefGetAddressRet handle_tex_ref_get_address(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUdeviceptr real_pdptr = 0;
    CUresult res = cuTexRefGetAddress(&real_pdptr, real_hTexRef);
    TexRefGetAddressRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pdptr = static_cast<uint64_t>(real_pdptr);
    if (res == CUDA_SUCCESS && real_pdptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuTexRefGetAddress_v2 (server handler) ──
// cuTexRefGetAddress_v2 → real CUDA call on GPU
static TexRefGetAddressV2Ret handle_tex_ref_get_address_v2(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUdeviceptr real_pdptr = 0;
    CUresult res = cuTexRefGetAddress_v2(&real_pdptr, real_hTexRef);
    TexRefGetAddressV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.pdptr = static_cast<uint64_t>(real_pdptr);
    if (res == CUDA_SUCCESS && real_pdptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuTexRefGetFormat (server handler) ──
// cuTexRefGetFormat → real CUDA call on GPU
static TexRefGetFormatRet handle_tex_ref_get_format(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    unsigned int real_pfmt = 0;
    int real_pNumPackedComponents = 0;
    CUresult res = cuTexRefGetFormat(&real_pfmt, &real_pNumPackedComponents, real_hTexRef);
    TexRefGetFormatRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pfmt = static_cast<uint32_t>(real_pfmt);
    ret.pNumPackedComponents = static_cast<int32_t>(real_pNumPackedComponents);
    return ret;
}

// ── cuTexRefGetAddressMode (server handler) ──
// cuTexRefGetAddressMode → real CUDA call on GPU
static TexRefGetAddressModeRet handle_tex_ref_get_address_mode(uint64_t hTexRef, int32_t dim) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    unsigned int real_pam = 0;
    CUresult res = cuTexRefGetAddressMode(&real_pam, real_hTexRef, dim);
    TexRefGetAddressModeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pam = static_cast<uint32_t>(real_pam);
    return ret;
}

// ── cuTexRefGetFilterMode (server handler) ──
// cuTexRefGetFilterMode → real CUDA call on GPU
static TexRefGetFilterModeRet handle_tex_ref_get_filter_mode(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    unsigned int real_pfm = 0;
    CUresult res = cuTexRefGetFilterMode(&real_pfm, real_hTexRef);
    TexRefGetFilterModeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pfm = static_cast<uint32_t>(real_pfm);
    return ret;
}

// ── cuTexRefGetMipmapFilterMode (server handler) ──
// cuTexRefGetMipmapFilterMode → real CUDA call on GPU
static TexRefGetMipmapFilterModeRet handle_tex_ref_get_mipmap_filter_mode(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    unsigned int real_pfm = 0;
    CUresult res = cuTexRefGetMipmapFilterMode(&real_pfm, real_hTexRef);
    TexRefGetMipmapFilterModeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pfm = static_cast<uint32_t>(real_pfm);
    return ret;
}

// ── cuTexRefGetMipmapLevelBias (server handler) ──
// cuTexRefGetMipmapLevelBias → real CUDA call on GPU
static TexRefGetMipmapLevelBiasRet handle_tex_ref_get_mipmap_level_bias(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    float real_pbias = 0;
    CUresult res = cuTexRefGetMipmapLevelBias(&real_pbias, real_hTexRef);
    TexRefGetMipmapLevelBiasRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pbias = real_pbias;
    return ret;
}

// ── cuTexRefGetMipmapLevelClamp (server handler) ──
// cuTexRefGetMipmapLevelClamp → real CUDA call on GPU
static TexRefGetMipmapLevelClampRet handle_tex_ref_get_mipmap_level_clamp(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    float real_pminMipmapLevelClamp = 0;
    float real_pmaxMipmapLevelClamp = 0;
    CUresult res = cuTexRefGetMipmapLevelClamp(&real_pminMipmapLevelClamp, &real_pmaxMipmapLevelClamp, real_hTexRef);
    TexRefGetMipmapLevelClampRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pminMipmapLevelClamp = real_pminMipmapLevelClamp;
    ret.pmaxMipmapLevelClamp = real_pmaxMipmapLevelClamp;
    return ret;
}

// ── cuTexRefGetMaxAnisotropy (server handler) ──
// cuTexRefGetMaxAnisotropy → real CUDA call on GPU
static TexRefGetMaxAnisotropyRet handle_tex_ref_get_max_anisotropy(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    int real_pmaxAniso = 0;
    CUresult res = cuTexRefGetMaxAnisotropy(&real_pmaxAniso, real_hTexRef);
    TexRefGetMaxAnisotropyRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pmaxAniso = static_cast<int32_t>(real_pmaxAniso);
    return ret;
}

// ── cuTexRefGetBorderColor (server handler) ──
// cuTexRefGetBorderColor → real CUDA call on GPU
static TexRefGetBorderColorRet handle_tex_ref_get_border_color(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    CUresult res = cuTexRefGetBorderColor(&real_pBorderColor, real_hTexRef);
    TexRefGetBorderColorRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuTexRefGetFlags (server handler) ──
// cuTexRefGetFlags → real CUDA call on GPU
static TexRefGetFlagsRet handle_tex_ref_get_flags(uint64_t hTexRef) {
    CUtexref real_hTexRef = reinterpret_cast<CUtexref>(g_vhandles.translate(hTexRef));
    unsigned int real_pFlags = 0;
    CUresult res = cuTexRefGetFlags(&real_pFlags, real_hTexRef);
    TexRefGetFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pFlags = static_cast<uint32_t>(real_pFlags);
    return ret;
}

// ── cuSurfRefSetArray (server handler) ──
// cuSurfRefSetArray → real CUDA call on GPU
static SurfRefSetArrayRet handle_surf_ref_set_array(uint64_t hSurfRef, uint64_t hArray, uint32_t Flags) {
    CUsurfref real_hSurfRef = reinterpret_cast<CUsurfref>(g_vhandles.translate(hSurfRef));
    CUarray real_hArray = reinterpret_cast<CUarray>(g_vhandles.translate(hArray));
    CUresult res = cuSurfRefSetArray(real_hSurfRef, real_hArray, static_cast<unsigned int>(Flags));
    SurfRefSetArrayRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuSurfRefGetArray (server handler) ──
// cuSurfRefGetArray → real CUDA call on GPU
static SurfRefGetArrayRet handle_surf_ref_get_array(uint64_t hSurfRef) {
    CUsurfref real_hSurfRef = reinterpret_cast<CUsurfref>(g_vhandles.translate(hSurfRef));
    CUarray real_phArray = nullptr;
    CUresult res = cuSurfRefGetArray(&real_phArray, real_hSurfRef);
    SurfRefGetArrayRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phArray = reinterpret_cast<uint64_t>(real_phArray);
    if (res == CUDA_SUCCESS && real_phArray != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuArrayDestroy (server handler) ──
// cuArrayDestroy → real CUDA call on GPU
static ArrayDestroyRet handle_array_destroy(uint64_t hArray) {
    CUarray real_hArray = reinterpret_cast<CUarray>(g_vhandles.translate(hArray));
    CUresult res = cuArrayDestroy(real_hArray);
    ArrayDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMipmappedArrayGetLevel (server handler) ──
// cuMipmappedArrayGetLevel → real CUDA call on GPU
static MipmappedArrayGetLevelRet handle_mipmapped_array_get_level(uint64_t hMipmappedArray, uint32_t level) {
    CUmipmappedArray real_hMipmappedArray = reinterpret_cast<CUmipmappedArray>(g_vhandles.translate(hMipmappedArray));
    CUarray real_pLevelArray = nullptr;
    CUresult res = cuMipmappedArrayGetLevel(&real_pLevelArray, real_hMipmappedArray, static_cast<unsigned int>(level));
    MipmappedArrayGetLevelRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pLevelArray = reinterpret_cast<uint64_t>(real_pLevelArray);
    if (res == CUDA_SUCCESS && real_pLevelArray != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMipmappedArrayDestroy (server handler) ──
// cuMipmappedArrayDestroy → real CUDA call on GPU
static MipmappedArrayDestroyRet handle_mipmapped_array_destroy(uint64_t hMipmappedArray) {
    CUmipmappedArray real_hMipmappedArray = reinterpret_cast<CUmipmappedArray>(g_vhandles.translate(hMipmappedArray));
    CUresult res = cuMipmappedArrayDestroy(real_hMipmappedArray);
    MipmappedArrayDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuPointerGetAttribute (server handler) ──
// cuPointerGetAttribute → real CUDA call on GPU
static PointerGetAttributeRet handle_pointer_get_attribute(uint32_t attribute, uint64_t ptr) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    CUresult res = cuPointerGetAttribute(&real_data, static_cast<unsigned int>(attribute), real_ptr);
    PointerGetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuPointerSetAttribute (server handler) ──
// cuPointerSetAttribute → real CUDA call on GPU
static PointerSetAttributeRet handle_pointer_set_attribute(uint64_t value, uint32_t attribute, uint64_t ptr) {
    CUdeviceptr real_ptr = static_cast<CUdeviceptr>(g_vhandles.translate(ptr));
    CUresult res = cuPointerSetAttribute(value, static_cast<unsigned int>(attribute), real_ptr);
    PointerSetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuDeviceCanAccessPeer (server handler) ──
// cuDeviceCanAccessPeer → real CUDA call on GPU
static DeviceCanAccessPeerRet handle_device_can_access_peer(int32_t dev, int32_t peerDev) {
    int real_canAccessPeer = 0;
    CUresult res = cuDeviceCanAccessPeer(&real_canAccessPeer, static_cast<CUdevice>(dev), static_cast<CUdevice>(peerDev));
    DeviceCanAccessPeerRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.canAccessPeer = static_cast<int32_t>(real_canAccessPeer);
    return ret;
}

// ── cuCtxEnablePeerAccess (server handler) ──
// cuCtxEnablePeerAccess → real CUDA call on GPU
static CtxEnablePeerAccessRet handle_ctx_enable_peer_access(uint64_t peerContext, uint32_t Flags) {
    CUcontext real_peerContext = reinterpret_cast<CUcontext>(g_vhandles.translate(peerContext));
    CUresult res = cuCtxEnablePeerAccess(real_peerContext, static_cast<unsigned int>(Flags));
    CtxEnablePeerAccessRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxDisablePeerAccess (server handler) ──
// cuCtxDisablePeerAccess → real CUDA call on GPU
static CtxDisablePeerAccessRet handle_ctx_disable_peer_access(uint64_t peerContext) {
    CUcontext real_peerContext = reinterpret_cast<CUcontext>(g_vhandles.translate(peerContext));
    CUresult res = cuCtxDisablePeerAccess(real_peerContext);
    CtxDisablePeerAccessRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphCreate (server handler) ──
// cuGraphCreate → real CUDA call on GPU
static GraphCreateRet handle_graph_create(uint32_t flags) {
    CUgraph real_phGraph = nullptr;
    CUresult res = cuGraphCreate(&real_phGraph, static_cast<unsigned int>(flags));
    GraphCreateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraph = reinterpret_cast<uint64_t>(real_phGraph);
    if (res == CUDA_SUCCESS && real_phGraph != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphDestroy (server handler) ──
// cuGraphDestroy → real CUDA call on GPU
static GraphDestroyRet handle_graph_destroy(uint64_t hGraph) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUresult res = cuGraphDestroy(real_hGraph);
    GraphDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphAddMemcpyNode1D (server handler) ──
// cuGraphAddMemcpyNode1D → real CUDA call on GPU
static GraphAddMemcpyNode1DRet handle_graph_add_memcpy_node1_d(uint64_t hGraph, uint64_t dependencies, uint64_t numDependencies, uint64_t dst, uint64_t src, uint64_t count, uint64_t ctx) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUdeviceptr real_dst = static_cast<CUdeviceptr>(g_vhandles.translate(dst));
    CUdeviceptr real_src = static_cast<CUdeviceptr>(g_vhandles.translate(src));
    CUcontext real_ctx = reinterpret_cast<CUcontext>(g_vhandles.translate(ctx));
    CUgraphNode real_phGraphNode = nullptr;
    CUresult res = cuGraphAddMemcpyNode1D(&real_phGraphNode, real_hGraph, dependencies, static_cast<size_t>(numDependencies), real_dst, real_src, static_cast<size_t>(count), real_ctx);
    GraphAddMemcpyNode1DRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphNode = reinterpret_cast<uint64_t>(real_phGraphNode);
    if (res == CUDA_SUCCESS && real_phGraphNode != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphAddEmptyNode (server handler) ──
// cuGraphAddEmptyNode → real CUDA call on GPU
static GraphAddEmptyNodeRet handle_graph_add_empty_node(uint64_t hGraph, uint64_t dependencies, uint64_t numDependencies) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUgraphNode real_phGraphNode = nullptr;
    CUresult res = cuGraphAddEmptyNode(&real_phGraphNode, real_hGraph, dependencies, static_cast<size_t>(numDependencies));
    GraphAddEmptyNodeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphNode = reinterpret_cast<uint64_t>(real_phGraphNode);
    if (res == CUDA_SUCCESS && real_phGraphNode != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphAddEventRecordNode (server handler) ──
// cuGraphAddEventRecordNode → real CUDA call on GPU
static GraphAddEventRecordNodeRet handle_graph_add_event_record_node(uint64_t hGraph, uint64_t dependencies, uint64_t numDependencies, uint64_t hEvent) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUgraphNode real_phGraphNode = nullptr;
    CUresult res = cuGraphAddEventRecordNode(&real_phGraphNode, real_hGraph, dependencies, static_cast<size_t>(numDependencies), real_hEvent);
    GraphAddEventRecordNodeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphNode = reinterpret_cast<uint64_t>(real_phGraphNode);
    if (res == CUDA_SUCCESS && real_phGraphNode != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphAddEventWaitNode (server handler) ──
// cuGraphAddEventWaitNode → real CUDA call on GPU
static GraphAddEventWaitNodeRet handle_graph_add_event_wait_node(uint64_t hGraph, uint64_t dependencies, uint64_t numDependencies, uint64_t hEvent) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUgraphNode real_phGraphNode = nullptr;
    CUresult res = cuGraphAddEventWaitNode(&real_phGraphNode, real_hGraph, dependencies, static_cast<size_t>(numDependencies), real_hEvent);
    GraphAddEventWaitNodeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphNode = reinterpret_cast<uint64_t>(real_phGraphNode);
    if (res == CUDA_SUCCESS && real_phGraphNode != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphAddChildGraphNode (server handler) ──
// cuGraphAddChildGraphNode → real CUDA call on GPU
static GraphAddChildGraphNodeRet handle_graph_add_child_graph_node(uint64_t hGraph, uint64_t dependencies, uint64_t numDependencies, uint64_t childGraph) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUgraph real_childGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(childGraph));
    CUgraphNode real_phGraphNode = nullptr;
    CUresult res = cuGraphAddChildGraphNode(&real_phGraphNode, real_hGraph, dependencies, static_cast<size_t>(numDependencies), real_childGraph);
    GraphAddChildGraphNodeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphNode = reinterpret_cast<uint64_t>(real_phGraphNode);
    if (res == CUDA_SUCCESS && real_phGraphNode != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphNodeGetType (server handler) ──
// cuGraphNodeGetType → real CUDA call on GPU
static GraphNodeGetTypeRet handle_graph_node_get_type(uint64_t hNode) {
    CUgraphNode real_hNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hNode));
    unsigned int real_type = 0;
    CUresult res = cuGraphNodeGetType(real_hNode, &real_type);
    GraphNodeGetTypeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.type = static_cast<uint32_t>(real_type);
    return ret;
}

// ── cuGraphDestroyNode (server handler) ──
// cuGraphDestroyNode → real CUDA call on GPU
static GraphDestroyNodeRet handle_graph_destroy_node(uint64_t hNode) {
    CUgraphNode real_hNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hNode));
    CUresult res = cuGraphDestroyNode(real_hNode);
    GraphDestroyNodeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphInstantiateWithFlags (server handler) ──
// cuGraphInstantiateWithFlags → real CUDA call on GPU
static GraphInstantiateWithFlagsRet handle_graph_instantiate_with_flags(uint64_t hGraph, uint64_t flags) {
    CUgraph real_hGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hGraph));
    CUgraphExec real_phGraphExec = nullptr;
    CUresult res = cuGraphInstantiateWithFlags(&real_phGraphExec, real_hGraph, static_cast<unsigned long long>(flags));
    GraphInstantiateWithFlagsRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphExec = reinterpret_cast<uint64_t>(real_phGraphExec);
    if (res == CUDA_SUCCESS && real_phGraphExec != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphLaunch (server handler) ──
// cuGraphLaunch → real CUDA call on GPU
static GraphLaunchRet handle_graph_launch(uint64_t hGraphExec, uint64_t hStream) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuGraphLaunch(real_hGraphExec, real_hStream);
    GraphLaunchRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphLaunch_ptsz (server handler) ──
// cuGraphLaunch_ptsz → real CUDA call on GPU
static GraphLaunchPtszRet handle_graph_launch_ptsz(uint64_t hGraphExec, uint64_t hStream) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuGraphLaunch_ptsz(real_hGraphExec, real_hStream);
    GraphLaunchPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphExecDestroy (server handler) ──
// cuGraphExecDestroy → real CUDA call on GPU
static GraphExecDestroyRet handle_graph_exec_destroy(uint64_t hGraphExec) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUresult res = cuGraphExecDestroy(real_hGraphExec);
    GraphExecDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphClone (server handler) ──
// cuGraphClone → real CUDA call on GPU
static GraphCloneRet handle_graph_clone(uint64_t originalGraph) {
    CUgraph real_originalGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(originalGraph));
    CUgraph real_phGraphClone = nullptr;
    CUresult res = cuGraphClone(&real_phGraphClone, real_originalGraph);
    GraphCloneRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phGraphClone = reinterpret_cast<uint64_t>(real_phGraphClone);
    if (res == CUDA_SUCCESS && real_phGraphClone != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphNodeFindInClone (server handler) ──
// cuGraphNodeFindInClone → real CUDA call on GPU
static GraphNodeFindInCloneRet handle_graph_node_find_in_clone(uint64_t hOriginalNode, uint64_t hClonedGraph) {
    CUgraphNode real_hOriginalNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hOriginalNode));
    CUgraph real_hClonedGraph = reinterpret_cast<CUgraph>(g_vhandles.translate(hClonedGraph));
    CUgraphNode real_phNode = nullptr;
    CUresult res = cuGraphNodeFindInClone(&real_phNode, real_hOriginalNode, real_hClonedGraph);
    GraphNodeFindInCloneRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.phNode = reinterpret_cast<uint64_t>(real_phNode);
    if (res == CUDA_SUCCESS && real_phNode != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphExecEventRecordNodeSetEvent (server handler) ──
// cuGraphExecEventRecordNodeSetEvent → real CUDA call on GPU
static GraphExecEventRecordNodeSetEventRet handle_graph_exec_event_record_node_set_event(uint64_t hGraphExec, uint64_t hNode, uint64_t hEvent) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUgraphNode real_hNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hNode));
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuGraphExecEventRecordNodeSetEvent(real_hGraphExec, real_hNode, real_hEvent);
    GraphExecEventRecordNodeSetEventRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphExecEventWaitNodeSetEvent (server handler) ──
// cuGraphExecEventWaitNodeSetEvent → real CUDA call on GPU
static GraphExecEventWaitNodeSetEventRet handle_graph_exec_event_wait_node_set_event(uint64_t hGraphExec, uint64_t hNode, uint64_t hEvent) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUgraphNode real_hNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hNode));
    CUevent real_hEvent = reinterpret_cast<CUevent>(g_vhandles.translate(hEvent));
    CUresult res = cuGraphExecEventWaitNodeSetEvent(real_hGraphExec, real_hNode, real_hEvent);
    GraphExecEventWaitNodeSetEventRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphEventRecordNodeGetEvent (server handler) ──
// cuGraphEventRecordNodeGetEvent → real CUDA call on GPU
static GraphEventRecordNodeGetEventRet handle_graph_event_record_node_get_event(uint64_t hNode) {
    CUgraphNode real_hNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hNode));
    CUevent real_hEvent_out = nullptr;
    CUresult res = cuGraphEventRecordNodeGetEvent(real_hNode, &real_hEvent_out);
    GraphEventRecordNodeGetEventRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.hEvent_out = reinterpret_cast<uint64_t>(real_hEvent_out);
    if (res == CUDA_SUCCESS && real_hEvent_out != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphEventWaitNodeGetEvent (server handler) ──
// cuGraphEventWaitNodeGetEvent → real CUDA call on GPU
static GraphEventWaitNodeGetEventRet handle_graph_event_wait_node_get_event(uint64_t hNode) {
    CUgraphNode real_hNode = reinterpret_cast<CUgraphNode>(g_vhandles.translate(hNode));
    CUevent real_hEvent_out = nullptr;
    CUresult res = cuGraphEventWaitNodeGetEvent(real_hNode, &real_hEvent_out);
    GraphEventWaitNodeGetEventRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.hEvent_out = reinterpret_cast<uint64_t>(real_hEvent_out);
    if (res == CUDA_SUCCESS && real_hEvent_out != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuGraphUpload (server handler) ──
// cuGraphUpload → real CUDA call on GPU
static GraphUploadRet handle_graph_upload(uint64_t hGraphExec, uint64_t hStream) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuGraphUpload(real_hGraphExec, real_hStream);
    GraphUploadRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamWriteValue32 (server handler) ──
// cuStreamWriteValue32 → real CUDA call on GPU
static StreamWriteValue32Ret handle_stream_write_value32(uint64_t hStream, uint64_t addr, uint32_t value, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    CUresult res = cuStreamWriteValue32(real_hStream, real_addr, static_cast<unsigned int>(value), static_cast<unsigned int>(flags));
    StreamWriteValue32Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamWriteValue32_ptsz (server handler) ──
// cuStreamWriteValue32_ptsz → real CUDA call on GPU
static StreamWriteValue32PtszRet handle_stream_write_value32_ptsz(uint64_t hStream, uint64_t addr, uint32_t value, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    CUresult res = cuStreamWriteValue32_ptsz(real_hStream, real_addr, static_cast<unsigned int>(value), static_cast<unsigned int>(flags));
    StreamWriteValue32PtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamWriteValue64 (server handler) ──
// cuStreamWriteValue64 → real CUDA call on GPU
static StreamWriteValue64Ret handle_stream_write_value64(uint64_t hStream, uint64_t addr, uint64_t value, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    CUresult res = cuStreamWriteValue64(real_hStream, real_addr, static_cast<unsigned long long>(value), static_cast<unsigned int>(flags));
    StreamWriteValue64Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamWriteValue64_ptsz (server handler) ──
// cuStreamWriteValue64_ptsz → real CUDA call on GPU
static StreamWriteValue64PtszRet handle_stream_write_value64_ptsz(uint64_t hStream, uint64_t addr, uint64_t value, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    CUresult res = cuStreamWriteValue64_ptsz(real_hStream, real_addr, static_cast<unsigned long long>(value), static_cast<unsigned int>(flags));
    StreamWriteValue64PtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamReadValue32 (server handler) ──
// cuStreamReadValue32 → real CUDA call on GPU
static StreamReadValue32Ret handle_stream_read_value32(uint64_t hStream, uint64_t addr, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    unsigned int real_value = 0;
    CUresult res = cuStreamReadValue32(real_hStream, real_addr, &real_value, static_cast<unsigned int>(flags));
    StreamReadValue32Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<uint32_t>(real_value);
    return ret;
}

// ── cuStreamReadValue32_ptsz (server handler) ──
// cuStreamReadValue32_ptsz → real CUDA call on GPU
static StreamReadValue32PtszRet handle_stream_read_value32_ptsz(uint64_t hStream, uint64_t addr, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    unsigned int real_value = 0;
    CUresult res = cuStreamReadValue32_ptsz(real_hStream, real_addr, &real_value, static_cast<unsigned int>(flags));
    StreamReadValue32PtszRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<uint32_t>(real_value);
    return ret;
}

// ── cuStreamReadValue64 (server handler) ──
// cuStreamReadValue64 → real CUDA call on GPU
static StreamReadValue64Ret handle_stream_read_value64(uint64_t hStream, uint64_t addr, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    unsigned long long real_value = 0;
    CUresult res = cuStreamReadValue64(real_hStream, real_addr, &real_value, static_cast<unsigned int>(flags));
    StreamReadValue64Ret ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<uint64_t>(real_value);
    return ret;
}

// ── cuStreamReadValue64_ptsz (server handler) ──
// cuStreamReadValue64_ptsz → real CUDA call on GPU
static StreamReadValue64PtszRet handle_stream_read_value64_ptsz(uint64_t hStream, uint64_t addr, uint32_t flags) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_addr = static_cast<CUdeviceptr>(g_vhandles.translate(addr));
    unsigned long long real_value = 0;
    CUresult res = cuStreamReadValue64_ptsz(real_hStream, real_addr, &real_value, static_cast<unsigned int>(flags));
    StreamReadValue64PtszRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<uint64_t>(real_value);
    return ret;
}

// ── cuProfilerInitialize (server handler) ──
// cuProfilerInitialize → real CUDA call on GPU
static ProfilerInitializeRet handle_profiler_initialize(std::string configFile, std::string outputFile, uint32_t outputMode) {
    CUresult res = cuProfilerInitialize(configFile.c_str(), outputFile.c_str(), static_cast<unsigned int>(outputMode));
    ProfilerInitializeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuProfilerStart (server handler) ──
// cuProfilerStart → real CUDA call on GPU
static ProfilerStartRet handle_profiler_start() {
    CUresult res = cuProfilerStart();
    ProfilerStartRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuProfilerStop (server handler) ──
// cuProfilerStop → real CUDA call on GPU
static ProfilerStopRet handle_profiler_stop() {
    CUresult res = cuProfilerStop();
    ProfilerStopRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemAdvise (server handler) ──
// cuMemAdvise → real CUDA call on GPU
static MemAdviseRet handle_mem_advise(uint64_t dptr, uint64_t count, uint32_t advice, uint64_t location) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuMemAdvise(real_dptr, static_cast<size_t>(count), static_cast<unsigned int>(advice), location);
    MemAdviseRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemAdvise_v2 (server handler) ──
// cuMemAdvise_v2 → real CUDA call on GPU
static MemAdviseV2Ret handle_mem_advise_v2(uint64_t dptr, uint64_t count, uint32_t advice, uint64_t location) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuMemAdvise_v2(real_dptr, static_cast<size_t>(count), static_cast<unsigned int>(advice), location);
    MemAdviseV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemPrefetchAsync (server handler) ──
// cuMemPrefetchAsync → real CUDA call on GPU
static MemPrefetchAsyncRet handle_mem_prefetch_async(uint64_t dptr, uint64_t count, uint64_t location, uint64_t hStream) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemPrefetchAsync(real_dptr, static_cast<size_t>(count), location, real_hStream);
    MemPrefetchAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemPrefetchAsync_v2 (server handler) ──
// cuMemPrefetchAsync_v2 → real CUDA call on GPU
static MemPrefetchAsyncV2Ret handle_mem_prefetch_async_v2(uint64_t dptr, uint64_t count, uint64_t location, uint32_t flags, uint64_t hStream) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemPrefetchAsync_v2(real_dptr, static_cast<size_t>(count), location, static_cast<unsigned int>(flags), real_hStream);
    MemPrefetchAsyncV2Ret ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemRangeGetAttribute (server handler) ──
// cuMemRangeGetAttribute → real CUDA call on GPU
static MemRangeGetAttributeRet handle_mem_range_get_attribute(uint64_t dataSize, uint32_t attribute, uint64_t dptr, uint64_t count) {
    CUdeviceptr real_dptr = static_cast<CUdeviceptr>(g_vhandles.translate(dptr));
    CUresult res = cuMemRangeGetAttribute(&real_data, static_cast<size_t>(dataSize), static_cast<unsigned int>(attribute), real_dptr, static_cast<size_t>(count));
    MemRangeGetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuFuncGetAttribute (server handler) ──
// cuFuncGetAttribute → real CUDA call on GPU
static FuncGetAttributeRet handle_func_get_attribute(uint32_t attrib, uint64_t hfunc) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    int real_value = 0;
    CUresult res = cuFuncGetAttribute(&real_value, static_cast<unsigned int>(attrib), real_hfunc);
    FuncGetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<int32_t>(real_value);
    return ret;
}

// ── cuFuncSetAttribute (server handler) ──
// cuFuncSetAttribute → real CUDA call on GPU
static FuncSetAttributeRet handle_func_set_attribute(uint64_t hfunc, uint32_t attrib, int32_t value) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    CUresult res = cuFuncSetAttribute(real_hfunc, static_cast<unsigned int>(attrib), value);
    FuncSetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuFuncSetCacheConfig (server handler) ──
// cuFuncSetCacheConfig → real CUDA call on GPU
static FuncSetCacheConfigRet handle_func_set_cache_config(uint64_t hfunc, uint32_t config) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    CUresult res = cuFuncSetCacheConfig(real_hfunc, static_cast<unsigned int>(config));
    FuncSetCacheConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuFuncSetSharedMemConfig (server handler) ──
// cuFuncSetSharedMemConfig → real CUDA call on GPU
static FuncSetSharedMemConfigRet handle_func_set_shared_mem_config(uint64_t hfunc, uint32_t config) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    CUresult res = cuFuncSetSharedMemConfig(real_hfunc, static_cast<unsigned int>(config));
    FuncSetSharedMemConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuFuncGetModule (server handler) ──
// cuFuncGetModule → real CUDA call on GPU
static FuncGetModuleRet handle_func_get_module(uint64_t hfunc) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    CUmodule real_hmod = nullptr;
    CUresult res = cuFuncGetModule(&real_hmod, real_hfunc);
    FuncGetModuleRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.hmod = reinterpret_cast<uint64_t>(real_hmod);
    if (res == CUDA_SUCCESS && real_hmod != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuFuncGetName (server handler) ──
// cuFuncGetName → real CUDA call on GPU
static FuncGetNameRet handle_func_get_name(uint64_t hfunc) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    CUresult res = cuFuncGetName(&real_name, real_hfunc);
    FuncGetNameRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuFuncIsLoaded (server handler) ──
// cuFuncIsLoaded → real CUDA call on GPU
static FuncIsLoadedRet handle_func_is_loaded(uint64_t hfunc) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    unsigned int real_state = 0;
    CUresult res = cuFuncIsLoaded(&real_state, real_hfunc);
    FuncIsLoadedRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.state = static_cast<uint32_t>(real_state);
    return ret;
}

// ── cuFuncLoad (server handler) ──
// cuFuncLoad → real CUDA call on GPU
static FuncLoadRet handle_func_load(uint64_t hfunc) {
    CUfunction real_hfunc = reinterpret_cast<CUfunction>(g_vhandles.translate(hfunc));
    CUresult res = cuFuncLoad(real_hfunc);
    FuncLoadRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuLibraryLoadData (server handler) ──
// cuLibraryLoadData → real CUDA call on GPU
static LibraryLoadDataRet handle_library_load_data(uint64_t code, uint64_t jitOptions, uint64_t jitOptionValues, uint32_t numJitOptions) {
    CUlibrary real_library = nullptr;
    CUresult res = cuLibraryLoadData(&real_library, code, jitOptions, jitOptionValues, static_cast<unsigned int>(numJitOptions));
    LibraryLoadDataRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.library = reinterpret_cast<uint64_t>(real_library);
    if (res == CUDA_SUCCESS && real_library != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuLibraryLoadFromFile (server handler) ──
// cuLibraryLoadFromFile → real CUDA call on GPU
static LibraryLoadFromFileRet handle_library_load_from_file(std::string fileName, uint64_t jitOptions, uint64_t jitOptionValues, uint32_t numJitOptions) {
    CUlibrary real_library = nullptr;
    CUresult res = cuLibraryLoadFromFile(&real_library, fileName.c_str(), jitOptions, jitOptionValues, static_cast<unsigned int>(numJitOptions));
    LibraryLoadFromFileRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.library = reinterpret_cast<uint64_t>(real_library);
    if (res == CUDA_SUCCESS && real_library != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuLibraryUnload (server handler) ──
// cuLibraryUnload → real CUDA call on GPU
static LibraryUnloadRet handle_library_unload(uint64_t library) {
    CUlibrary real_library = reinterpret_cast<CUlibrary>(g_vhandles.translate(library));
    CUresult res = cuLibraryUnload(real_library);
    LibraryUnloadRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuLibraryGetKernel (server handler) ──
// cuLibraryGetKernel → real CUDA call on GPU
static LibraryGetKernelRet handle_library_get_kernel(uint64_t library, std::string name) {
    CUlibrary real_library = reinterpret_cast<CUlibrary>(g_vhandles.translate(library));
    CUkernel real_pKernel = nullptr;
    CUresult res = cuLibraryGetKernel(&real_pKernel, real_library, name.c_str());
    LibraryGetKernelRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pKernel = reinterpret_cast<uint64_t>(real_pKernel);
    if (res == CUDA_SUCCESS && real_pKernel != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuLibraryGetModule (server handler) ──
// cuLibraryGetModule → real CUDA call on GPU
static LibraryGetModuleRet handle_library_get_module(uint64_t library) {
    CUlibrary real_library = reinterpret_cast<CUlibrary>(g_vhandles.translate(library));
    CUmodule real_pMod = nullptr;
    CUresult res = cuLibraryGetModule(&real_pMod, real_library);
    LibraryGetModuleRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pMod = reinterpret_cast<uint64_t>(real_pMod);
    if (res == CUDA_SUCCESS && real_pMod != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuKernelGetFunction (server handler) ──
// cuKernelGetFunction → real CUDA call on GPU
static KernelGetFunctionRet handle_kernel_get_function(uint64_t kernel) {
    CUkernel real_kernel = reinterpret_cast<CUkernel>(g_vhandles.translate(kernel));
    CUfunction real_pFunc = nullptr;
    CUresult res = cuKernelGetFunction(&real_pFunc, real_kernel);
    KernelGetFunctionRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pFunc = reinterpret_cast<uint64_t>(real_pFunc);
    if (res == CUDA_SUCCESS && real_pFunc != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuKernelGetAttribute (server handler) ──
// cuKernelGetAttribute → real CUDA call on GPU
static KernelGetAttributeRet handle_kernel_get_attribute(uint32_t attrib, uint64_t kernel) {
    CUkernel real_kernel = reinterpret_cast<CUkernel>(g_vhandles.translate(kernel));
    int real_value = 0;
    CUresult res = cuKernelGetAttribute(&real_value, static_cast<unsigned int>(attrib), real_kernel);
    KernelGetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.value = static_cast<int32_t>(real_value);
    return ret;
}

// ── cuKernelSetAttribute (server handler) ──
// cuKernelSetAttribute → real CUDA call on GPU
static KernelSetAttributeRet handle_kernel_set_attribute(uint32_t attrib, int32_t value, uint64_t kernel) {
    CUkernel real_kernel = reinterpret_cast<CUkernel>(g_vhandles.translate(kernel));
    CUresult res = cuKernelSetAttribute(static_cast<unsigned int>(attrib), value, real_kernel);
    KernelSetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuKernelSetCacheConfig (server handler) ──
// cuKernelSetCacheConfig → real CUDA call on GPU
static KernelSetCacheConfigRet handle_kernel_set_cache_config(uint64_t kernel, uint32_t config) {
    CUkernel real_kernel = reinterpret_cast<CUkernel>(g_vhandles.translate(kernel));
    CUresult res = cuKernelSetCacheConfig(real_kernel, static_cast<unsigned int>(config));
    KernelSetCacheConfigRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuLibraryGetGlobal (server handler) ──
// cuLibraryGetGlobal → real CUDA call on GPU
static LibraryGetGlobalRet handle_library_get_global(uint64_t library, std::string name) {
    CUlibrary real_library = reinterpret_cast<CUlibrary>(g_vhandles.translate(library));
    CUdeviceptr real_dptr = 0;
    size_t real_bytes = 0;
    CUresult res = cuLibraryGetGlobal(&real_dptr, &real_bytes, real_library, name.c_str());
    LibraryGetGlobalRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    ret.bytes = static_cast<uint64_t>(real_bytes);
    return ret;
}

// ── cuLibraryGetManaged (server handler) ──
// cuLibraryGetManaged → real CUDA call on GPU
static LibraryGetManagedRet handle_library_get_managed(uint64_t library, std::string name) {
    CUlibrary real_library = reinterpret_cast<CUlibrary>(g_vhandles.translate(library));
    CUdeviceptr real_dptr = 0;
    size_t real_bytes = 0;
    CUresult res = cuLibraryGetManaged(&real_dptr, &real_bytes, real_library, name.c_str());
    LibraryGetManagedRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    ret.bytes = static_cast<uint64_t>(real_bytes);
    return ret;
}

// ── cuLibraryGetUnifiedFunction (server handler) ──
// cuLibraryGetUnifiedFunction → real CUDA call on GPU
static LibraryGetUnifiedFunctionRet handle_library_get_unified_function(uint64_t library, std::string name) {
    CUlibrary real_library = reinterpret_cast<CUlibrary>(g_vhandles.translate(library));
    CUresult res = cuLibraryGetUnifiedFunction(&real_pFunc, real_library, name.c_str());
    LibraryGetUnifiedFunctionRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpy_ptds (server handler) ──
// cuMemcpy_ptds → real CUDA call on GPU
static MemcpyPtdsRet handle_memcpy_ptds(uint64_t dst, uint64_t src, uint64_t ByteCount) {
    CUdeviceptr real_dst = static_cast<CUdeviceptr>(g_vhandles.translate(dst));
    CUdeviceptr real_src = static_cast<CUdeviceptr>(g_vhandles.translate(src));
    CUresult res = cuMemcpy_ptds(real_dst, real_src, static_cast<size_t>(ByteCount));
    MemcpyPtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyAsync_ptds (server handler) ──
// cuMemcpyAsync_ptds → real CUDA call on GPU
static MemcpyAsyncPtdsRet handle_memcpy_async_ptds(uint64_t dst, uint64_t src, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dst = static_cast<CUdeviceptr>(g_vhandles.translate(dst));
    CUdeviceptr real_src = static_cast<CUdeviceptr>(g_vhandles.translate(src));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyAsync_ptds(real_dst, real_src, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyAsyncPtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyHtoD_ptds (server handler) ──
// cuMemcpyHtoD_ptds → real CUDA call on GPU
static MemcpyHtoDPtdsRet handle_memcpy_hto_d_ptds(uint64_t dstDevice, uint64_t srcHost, uint64_t ByteCount) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemcpyHtoD_ptds(real_dstDevice, srcHost, static_cast<size_t>(ByteCount));
    MemcpyHtoDPtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoH_ptds (server handler) ──
// cuMemcpyDtoH_ptds → real CUDA call on GPU
static MemcpyDtoHPtdsRet handle_memcpy_dto_h_ptds(uint64_t srcDevice, uint64_t ByteCount) {
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUresult res = cuMemcpyDtoH_ptds(&real_dstHost, real_srcDevice, static_cast<size_t>(ByteCount));
    MemcpyDtoHPtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyHtoDAsync_ptsz (server handler) ──
// cuMemcpyHtoDAsync_ptsz → real CUDA call on GPU
static MemcpyHtoDAsyncPtszRet handle_memcpy_hto_d_async_ptsz(uint64_t dstDevice, uint64_t srcHost, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyHtoDAsync_ptsz(real_dstDevice, srcHost, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyHtoDAsyncPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemcpyDtoHAsync_ptsz (server handler) ──
// cuMemcpyDtoHAsync_ptsz → real CUDA call on GPU
static MemcpyDtoHAsyncPtszRet handle_memcpy_dto_h_async_ptsz(uint64_t srcDevice, uint64_t ByteCount, uint64_t hStream) {
    CUdeviceptr real_srcDevice = static_cast<CUdeviceptr>(g_vhandles.translate(srcDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemcpyDtoHAsync_ptsz(&real_dstHost, real_srcDevice, static_cast<size_t>(ByteCount), real_hStream);
    MemcpyDtoHAsyncPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD8_ptds (server handler) ──
// cuMemsetD8_ptds → real CUDA call on GPU
static MemsetD8PtdsRet handle_memset_d8_ptds(uint64_t dstDevice, uint32_t uc, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD8_ptds(real_dstDevice, static_cast<unsigned int>(uc), static_cast<size_t>(N));
    MemsetD8PtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD32_ptds (server handler) ──
// cuMemsetD32_ptds → real CUDA call on GPU
static MemsetD32PtdsRet handle_memset_d32_ptds(uint64_t dstDevice, uint32_t ui, uint64_t N) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUresult res = cuMemsetD32_ptds(real_dstDevice, static_cast<unsigned int>(ui), static_cast<size_t>(N));
    MemsetD32PtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD8Async_ptsz (server handler) ──
// cuMemsetD8Async_ptsz → real CUDA call on GPU
static MemsetD8AsyncPtszRet handle_memset_d8_async_ptsz(uint64_t dstDevice, uint32_t uc, uint64_t N, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD8Async_ptsz(real_dstDevice, static_cast<unsigned int>(uc), static_cast<size_t>(N), real_hStream);
    MemsetD8AsyncPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemsetD32Async_ptsz (server handler) ──
// cuMemsetD32Async_ptsz → real CUDA call on GPU
static MemsetD32AsyncPtszRet handle_memset_d32_async_ptsz(uint64_t dstDevice, uint32_t ui, uint64_t N, uint64_t hStream) {
    CUdeviceptr real_dstDevice = static_cast<CUdeviceptr>(g_vhandles.translate(dstDevice));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuMemsetD32Async_ptsz(real_dstDevice, static_cast<unsigned int>(ui), static_cast<size_t>(N), real_hStream);
    MemsetD32AsyncPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuStreamSynchronize_ptsz (server handler) ──
// cuStreamSynchronize_ptsz → real CUDA call on GPU
static StreamSynchronizePtszRet handle_stream_synchronize_ptsz(uint64_t hStream) {
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuStreamSynchronize_ptsz(real_hStream);
    StreamSynchronizePtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuCtxSynchronize_ptds (server handler) ──
// cuCtxSynchronize_ptds → real CUDA call on GPU
static CtxSynchronizePtdsRet handle_ctx_synchronize_ptds() {
    CUresult res = cuCtxSynchronize_ptds();
    CtxSynchronizePtdsRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuGraphLaunch_ptsz (server handler) ──
// cuGraphLaunch_ptsz → real CUDA call on GPU
static GraphLaunchPtszRet handle_graph_launch_ptsz(uint64_t hGraphExec, uint64_t hStream) {
    CUgraphExec real_hGraphExec = reinterpret_cast<CUgraphExec>(g_vhandles.translate(hGraphExec));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUresult res = cuGraphLaunch_ptsz(real_hGraphExec, real_hStream);
    GraphLaunchPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemAllocFromPoolAsync (server handler) ──
// cuMemAllocFromPoolAsync → real CUDA call on GPU
static MemAllocFromPoolAsyncRet handle_mem_alloc_from_pool_async(uint64_t bytesize, uint64_t pool, uint64_t hStream) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_dptr = 0;
    CUresult res = cuMemAllocFromPoolAsync(&real_dptr, static_cast<size_t>(bytesize), real_pool, real_hStream);
    MemAllocFromPoolAsyncRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemAllocFromPoolAsync_ptsz (server handler) ──
// cuMemAllocFromPoolAsync_ptsz → real CUDA call on GPU
static MemAllocFromPoolAsyncPtszRet handle_mem_alloc_from_pool_async_ptsz(uint64_t bytesize, uint64_t pool, uint64_t hStream) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUstream real_hStream = reinterpret_cast<CUstream>(g_vhandles.translate(hStream));
    CUdeviceptr real_dptr = 0;
    CUresult res = cuMemAllocFromPoolAsync_ptsz(&real_dptr, static_cast<size_t>(bytesize), real_pool, real_hStream);
    MemAllocFromPoolAsyncPtszRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.dptr = static_cast<uint64_t>(real_dptr);
    if (res == CUDA_SUCCESS && real_dptr != 0) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemPoolTrimTo (server handler) ──
// cuMemPoolTrimTo → real CUDA call on GPU
static MemPoolTrimToRet handle_mem_pool_trim_to(uint64_t pool, uint64_t minBytesToKeep) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUresult res = cuMemPoolTrimTo(real_pool, static_cast<size_t>(minBytesToKeep));
    MemPoolTrimToRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemPoolSetAttribute (server handler) ──
// cuMemPoolSetAttribute → real CUDA call on GPU
static MemPoolSetAttributeRet handle_mem_pool_set_attribute(uint64_t pool, uint32_t attr, uint64_t value) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUresult res = cuMemPoolSetAttribute(real_pool, static_cast<unsigned int>(attr), value);
    MemPoolSetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemPoolGetAttribute (server handler) ──
// cuMemPoolGetAttribute → real CUDA call on GPU
static MemPoolGetAttributeRet handle_mem_pool_get_attribute(uint64_t pool, uint32_t attr) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUresult res = cuMemPoolGetAttribute(real_pool, static_cast<unsigned int>(attr), &real_value);
    MemPoolGetAttributeRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}

// ── cuMemPoolCreate (server handler) ──
// cuMemPoolCreate → real CUDA call on GPU
static MemPoolCreateRet handle_mem_pool_create(uint64_t poolProps) {
    CUmemGenericAllocationHandle real_pool = nullptr;
    CUresult res = cuMemPoolCreate(&real_pool, poolProps);
    MemPoolCreateRet ret;
    ret.result = static_cast<int32_t>(res);
    ret.pool = reinterpret_cast<uint64_t>(real_pool);
    if (res == CUDA_SUCCESS && real_pool != nullptr) {
        g_has_produced_handle = true;
    }
    return ret;
}

// ── cuMemPoolDestroy (server handler) ──
// cuMemPoolDestroy → real CUDA call on GPU
static MemPoolDestroyRet handle_mem_pool_destroy(uint64_t pool) {
    CUmemGenericAllocationHandle real_pool = reinterpret_cast<CUmemGenericAllocationHandle>(g_vhandles.translate(pool));
    CUresult res = cuMemPoolDestroy(real_pool);
    MemPoolDestroyRet ret;
    ret.result = static_cast<int32_t>(res);
    return ret;
}
